{
  "run_name": "mailroom-rebuild",
  "workdir": "/home/ankit114/ringer-work/mailroom-rebuild-r1b",
  "repo": "/mnt/d_drive/repos/loop-factory",
  "worktrees": true,
  "max_parallel": 1,
  "tasks": [
    {
      "key": "stall-sensor-r1b",
      "engine": "codex",
      "task_type": "code-feature",
      "timeout_s": 2400,
      "engine_args": [
        "-c",
        "model_reasoning_effort=high"
      ],
      "spec": "You are a repo feature worker. Your current working directory IS a git worktree of the loop-factory repository, detached at HEAD. Edit files here directly.\n\nOWNERSHIP: you own exactly three paths, factory/outbox_push.py (existing), tests/test_outbox_push_stall.py (new), and tests/test_outbox_push.py (existing, and you WILL need to edit it, see below). Do NOT modify, create, delete, or reformat anything else under factory/, tests/, kernel/, runbooks/, departments/, or loopfactory.py. In particular do NOT touch factory/outbox_listen.py; another worker owns adjacent files this round. Do NOT git commit, do NOT git push, do NOT run git add -A. Leave your changes uncommitted; the check exports them.\n\nCONTEXT, AND THIS IS THE WHOLE POINT. factory/outbox_push.py watches a list of files named in its config and turns new lines into Linear cards plus a phone notification for the owner. On 2026-08-02 its config was edited so that the only path it watched no longer existed. For six days the timer fired every fifteen minutes, found nothing, and exited 0. Every dashboard was green while the human approval channel was completely dead. Nobody noticed. You are building the sensor that makes that impossible.\n\nREAD FIRST, READ-ONLY, for conventions: factory/outbox_push.py (the file you are changing; note tick(), the existing exit code 3 for a total ping-sender failure, and ConfigError with exit 2), tests/test_outbox_push.py (test style and the existing exit-code expectations), CLAUDE.md (repo rules, especially rule 2, deny by default, and the ban on allow-on-failure paths).\n\nBUILD THIS BEHAVIOUR:\n1. A tick in which EVERY configured watch path is missing from disk is a stall, not a success. Exit with a distinct nonzero code and print a message that names the missing path or paths, so the operator can fix it without reading the source.\n2. A tick in which AT LEAST ONE configured watch path exists is not a stall, even if that file is empty and there is nothing to push. It must still exit 0. An idle mailroom is healthy; a blind one is not.\n3. --dry-run must keep its current meaning and must not be turned into a stall failure by your change.\n4. Choose an exit code that does not collide with the existing meanings (2 is config error, 3 is total send failure). Document the code you chose in the module docstring alongside the others.\n5. Do not weaken or reorder any existing behaviour: cursor and row-hash dedup, the ledger append, the ping-then-card ordering, and the existing exit 3 path must all still work exactly as they do now.\n\nWRITE tests/test_outbox_push_stall.py, covering: all-paths-missing exits nonzero and names a missing path; one-path-present-and-empty exits 0; dry-run is unaffected. Follow the subprocess style in tests/test_outbox_push.py. Your tests are not the acceptance gate; an independent oracle drives the CLI through fixtures you have not seen.\n\nVERIFY before you finish, from the worktree root:\n  PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests -q -p no:cacheprovider\nThe whole suite must stay green.\n\nTHE ONE KNOWN CONFLICT, and handling it correctly is the point of this task. tests/test_outbox_push.py contains test_missing_watch_file_is_tolerated, which configures a SINGLE watch path that does not exist and asserts exit 0. That test encodes the OLD contract, and the old contract is the defect: a mailroom watching only a path that does not exist is blind, not idle. Under the new rule that single-missing-path config is a stall. You must update that test to the new contract rather than work around it. Do NOT delete it and do NOT weaken it into a tautology. Rename it to say what is now true, assert the stall exit code and that the message names the missing path, and ADD a sibling test proving the tolerant half still holds: two watch paths where one exists (empty is fine) and one is missing must still exit 0 and must still process the existing one. Preserve the original test's other assertion, that no cursor file is written when nothing was pushed, wherever it still applies.\n\nOUTPUT CONTRACT: leave the two owned files edited and uncommitted, and write ./notes.md at the ROOT of your worktree (the check copies it out before the worktree is deleted, and it is not a repo file so it will not trip the ownership gate) recording what you read, the exit code you chose and why, the verification command and its result, and any assumption.\n\nHARD RULES: never add an allow-on-failure path. No new dependencies. No network. Do not load skills or call MCP/Apps. Do not capture anything to any memory or brain service. State assumptions in notes.md rather than widening your ownership.",
      "check": "python3 '/mnt/d_drive/repos/loop-factory/ringer/mailroom-rebuild/checks/mailroom_check.py' --task stall --export '/home/ankit114/ringer-work/mailroom-rebuild-r1b/patches' --notes '/home/ankit114/ringer-work/mailroom-rebuild-r1b/notes/stall-sensor-r1b-notes.md'",
      "verified": "an independent oracle confirmed that a config whose every watch path is missing exits nonzero and names a missing path, that a config with one existing empty watch path still exits 0, the full test suite still passes with the legacy test_missing_watch_file_is_tolerated migrated to the new contract rather than deleted, only owned files changed, and the patch was exported outside the worktree."
    }
  ]
}
