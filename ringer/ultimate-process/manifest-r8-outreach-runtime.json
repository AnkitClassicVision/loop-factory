{
  "run_name": "ultimate-process",
  "workdir": "/home/ankit114/ringer-work/outreach-runtime",
  "worktrees": true,
  "repo": "/mnt/d_drive/repos/loop-factory",
  "max_parallel": 1,
  "tasks": [
    {
      "key": "outreach-runtime",
      "engine": "codex",
      "task_type": "code-feature",
      "timeout_s": 4800,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo. Do NOT git commit/branch/push/stash. No MCP, no network, no skills; do not ask for approval — nobody is watching; fixture-only tests, no live services, no real systemctl probing in tests. NEVER read *.env/*secret*/*token*/*key*/*credential* files. NEVER edit departments/outreach/charter.yaml, interview/, knowledge/, creation/, or anything under runbooks/. READ FIRST (all inside this worktree): departments/outreach/charter.yaml (mission, objectives, send classes, floors), departments/outreach/procedural-graph.md + subgraphs.json (your build contract: nodes ORCH,N1,N2,N3,N4,N5,N6), departments/outreach/creation/tickets/D1-authority-split.md + D3-lane-inventory.md (the watch boundary and evidence sources), departments/podcast/runtime/sense_estate.py + estate.json (the PROVEN watchdog sensing pattern — imitate it), departments/podcast/runtime/podcast_daily.sh (the proven receipt-gated orchestrator with run_heal_phase/append_heal_failure conventions and v2 sidecar records), factory/runrecord.py, factory/human_in_the_loop.py. MISSION — Stage 9 (F3) of the outreach department: implement SG-GOVERN. (1) runtime/estate.json — the outreach watch registry per D1/D3: hsd-daily-operator + obe-fix-consumer as systemd_user_timers entries with evidence specs (ledger_path/receipt_glob/log_glob pointing at their REAL artifacts — inspect /mnt/d_drive/repos/hubspot-daily-2 READ-ONLY to find actual receipt/ledger paths; if a lane has no artifact, evidence 'timer_only' honestly), plus a sales_lanes section listing the open-engine sales lanes with their state evidence paths (/mnt/d_drive/repos/open-engine/departments/sales/state — runs-v2.jsonl landed there 2026-08-03). NEVER include the four podcast-owned obe-* units (scheduled-intent-sweeper, draft-bridge, approved-send-executor, context-sync) — the D1 boundary. (2) runtime/lane_sense.py (N1) — liveness per watched entry from timer state (via the launch.py systemd_user_probe path like podcast's sense_estate) + evidence freshness; --root parameterized; writes state/lane_sense.json atomically. (3) runtime/state_reconcile.py (N2) — reads configured funnel-ledger and HubSpot-evidence artifact paths from estate.json (config keys, fixture-swappable), computes outreach_state_drift as a mismatch COUNT with a per-mismatch reasons list; ALSO the escalation return-path reader: consumes replies/decisions for open asks in state/asks.jsonl and marks them harvested; absent-honest when evidence paths missing. (4) runtime/gate_monitor.py (N3) — reads voice-gate receipt globs + observed-send ledgers from config: send_class_integrity = count of observed sends that match NO declared charter class or lack a passing gate receipt (a human-approved send through the existing approval lanes IS approved_draft_execution class = in-class); also computes voice_gate_pass_rate for baselines when receipts exist. (5) runtime/queue_ager.py (N4) — approval-queue drafts older than 48h from configured queue paths -> approval_queue_aged count. (6) runtime/escalate.py (N5) — for each NEW unknown failure fingerprint from N1-N4 outputs: append ONE ask to state/asks.jsonl + a PII-free draft in state/outbox/ + a row via the factory.human_in_the_loop escalate mechanism into state/decisions_outbox.jsonl; once per fingerprint ever (dedupe ledger). (7) runtime/objectives_sensor.py (N6) — objectives-observed/v1 with the three hard zeros from N2/N3/N4 outputs (absent when the upstream output is missing, never 0), baselines appended to state/objective_baselines.jsonl. (8) REWRITE runtime/outreach_daily.sh — receipt-gated chain N1->N2->N3->N4->N5->N6, one validated v2 record per node via factory.runrecord (department outreach, external_actions_taken=0, cost flat_subscription/0), nonzero exit on any missing receipt or failed append — follow podcast_daily.sh conventions including a sys.path-safe invocation style; every python script gets the REPO_ROOT sys.path guard (house pattern in podcast runtime — standalone invocation must work). (9) Bind impls in subgraphs.json (N1-N6) and remove the runtime_node.py file + its untraced_allowed entry. All python stdlib-only, logging not print, zero model calls, zero network at runtime (systemctl probe via the launch path is local). TESTS: departments/outreach/tests/test_outreach_nodes.py, >=12 fixture tests in tmp dirs via --root/config injection: lane_sense liveness + stale detection; boundary test proving the four podcast-owned obe-* units are ABSENT from estate.json (read the real file); reconcile drift count + reasons; reconcile harvests a reply for an open ask; gate_monitor in-class approved send counts 0 violations; unmatched-class send counts 1; missing gate receipt counts 1; queue_ager aged draft counted, fresh not; escalate once-per-fingerprint (no dup on rerun); objectives absent-when-upstream-missing; every emitted v2 record passes factory.runrecord.validate_record; orchestrator halts at a failing node (no later records). Write ./evidence.md (real evidence paths found in both external repos, what is honestly timer_only/absent today). VERIFY: PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/outreach/tests -q -p no:cacheprovider && python3 loopfactory.py validate --name outreach.",
      "check": "python3 /mnt/d_drive/repos/loop-factory/ringer/one-true-master/checks/fix_with_tests.py --verify-command 'PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/outreach/tests -q -p no:cacheprovider && python3 loopfactory.py validate --name outreach' --patch /home/ankit114/ringer-work/outreach-runtime/outreach-runtime.patch --owned-files 'departments/outreach/runtime,departments/outreach/subgraphs.json,departments/outreach/tests,evidence.md' --min-new-tests 12",
      "expect_files": [],
      "verified": "SG-GOVERN implemented: boundary-respecting lane sensing, evidence reconciliation with executed ask return path, send-class integrity monitoring, receipt-gated v2-recorded orchestrator — validate PASS with full traceability"
    }
  ]
}
