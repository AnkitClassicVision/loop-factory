{
  "run_name": "ultimate-process",
  "workdir": "/home/ankit114/ringer-work/outreach-cal",
  "worktrees": true,
  "repo": "/mnt/d_drive/repos/loop-factory",
  "max_parallel": 1,
  "tasks": [
    {
      "key": "outreach-calibration",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo. Do NOT git commit/branch/push/stash. No MCP, no network, no skills; do not ask for approval — nobody is watching; fixture-only tests. NEVER read *.env/*secret*/*token*/*key*/*credential* files. NEVER edit charter.yaml, creation/, interview/, knowledge/, or runbooks/. TWO LIVE-FIRE DEFECTS from the outreach department's first real shadow cycle (the runtime is fresh — departments/outreach/runtime/). DEFECT A — objectives document shape violates the contract: runtime/objectives_sensor.py wrote {schema, ts, objectives:{...}, baselines:{...}} but the ratified contract (factory/objectives_verify.py — read it; the live gate output was 'WHY outreach values: must be an object') is EXACTLY {schema:'objectives-observed/v1', ts, values:{...}}: rename the observed map to values, remove the baselines key from this document entirely (baselines belong ONLY in state/objective_baselines.jsonl, which already exists), and when every value is absent still write values as an empty object. THE MISSING RED: the prior tests asserted the writer's own invented shape — add a test that EXECUTES the real gate (python3 -m factory.objectives_verify --name fixture --charter <fixture charter> --objectives-file <the sensor's actual output> plus --allow-unknown where appropriate) against sensor output for a green case, an all-absent case, and a breach case asserting exit codes and WHY lines. DEFECT B — escalation noise and opacity: the first live cycle emitted 31 asks, each with a useless summary like 'Unknown outreach governance failure in N3' and no lane, path, or reason. Fix runtime/escalate.py (and its N1-N4 finding inputs as needed): (1) every ask and its outbox draft carries lane id, failure class, the evidence path involved, and the observed condition — PII-free, no message bodies, paths and counts only; (2) FIRST-RUN BASELINE semantics (the cadence contract declares first_run: eof): when a watched lane has no prior sense state, a missing-evidence finding is recorded to state/first_run_baseline.jsonl as a baseline observation and does NOT escalate; escalations fire only on TRANSITIONS (previously-healthy lane now failing, or a lane still failing on the cycle after its baseline was recorded); (3) per-run direct-ask ceiling of 5 (the cadence digest cap) — findings beyond the ceiling fold into ONE digest ask summarizing the rest by lane and class (the podcast ping-ceiling pattern); (4) once-per-fingerprint dedupe and rerun idempotence must survive all of this. Do not change the orchestrator chain order, the v2 record emission, or any other node's contract. TESTS: extend departments/outreach/tests/test_outreach_nodes.py (extend, never delete), >=8 new: gate-executed green/all-absent/breach cases from DEFECT A; values always an object; baselines absent from the observed doc; first run with missing evidence -> zero escalations + baseline rows written; second cycle with a lane STILL failing -> exactly one ask, carrying lane+class+path+condition; healthy->failed transition escalates once; 12 simultaneous transition findings -> 5 direct asks + 1 digest ask, none lost; rerun adds nothing. Write ./evidence.md. VERIFY: PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/outreach/tests -q -p no:cacheprovider && python3 loopfactory.py validate --name outreach.",
      "check": "python3 /mnt/d_drive/repos/loop-factory/ringer/one-true-master/checks/fix_with_tests.py --verify-command 'PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/outreach/tests -q -p no:cacheprovider && python3 loopfactory.py validate --name outreach' --patch /home/ankit114/ringer-work/outreach-cal/outreach-cal.patch --owned-files 'departments/outreach/runtime,departments/outreach/tests,evidence.md' --min-new-tests 8",
      "expect_files": [],
      "verified": "the observed document matches the gate contract exactly (proven by executing the gate), and escalation is diagnostic, baseline-aware, transition-driven, and ceiling-capped"
    }
  ]
}
