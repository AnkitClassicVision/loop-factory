{
  "run_name": "podcast-funnel-controller",
  "workdir": "/home/ankit114/ringer-work/podcast-funnel-controller-r5",
  "worktrees": true,
  "repo": "/mnt/d_drive/repos/podcast",
  "max_parallel": 1,
  "tasks": [
    {
      "key": "freshness-deltas",
      "engine": "codex",
      "task_type": "code-feature",
      "timeout_s": 2700,
      "engine_args": ["-c", "model_reasoning_effort=high"],
      "spec": "Your current working directory IS a git worktree of the podcast repo. Edit files here directly. Do NOT git commit/branch/push/stash. No MCP, no network, no model calls, no skills; tests fixture-only with invented people.\n\nOWNED FILES (all exist at HEAD — read each fully before editing):\n  scripts/hubspot_roster.py\n  scripts/funnel_evidence.py\n  tests/test_hubspot_roster.py\n  tests/test_funnel_evidence.py\n\nOWNER DIRECTIVE (Ankit 2026-08-03): context gathering must be DYNAMIC every run — live queries, never a maintained list. Three freshness gaps remain; close exactly these, changing nothing else:\n\n1. STALE-CACHE BOUND (hubspot_roster.py + funnel_evidence.py): when a live fetch fails and the cached roster fallback is used, compute its age from fetched_at. If age > 24h, the degraded note must say 'STALE roster cache (<N>h old)' and funnel_evidence focus must treat it as a conservative signal: force mode no better than DEFICIT (i.e. if computed mode would be SURPLUS or NORMAL, downgrade to DEFICIT; CRITICAL stays CRITICAL) and record 'stale_roster_forced_deficit': true in FOCUS-LIST.json and the receipt. Fresh fetch or cache <= 24h: behavior unchanged.\n\n2. CANDIDATE-INBOX DEMOTION (funnel_evidence.py): episodes/CANDIDATE-INBOX.json stops being a roster/known-people source. It remains an INTAKE feed only: the ledger rebuild may still ingest its rows (do not change funnel_ledger.py — not owned), but the known-people index used to classify Gmail senders is built ONLY from the live HubSpot roster + FUNNEL-LEDGER.json. Update the module docstring to state: membership truth is the live HubSpot query; the candidate inbox is intake, not authority.\n\n3. OUTBOUND VISIBILITY (funnel_evidence.py): the Gmail collector currently sees only the inbox, so our own replies are invisible and a person we answered an hour ago still shows 'awaiting our first response' (live defect: a reply sent today to a roster person did not register). Fix: collect runs the gmail command TWICE — once as today (inbox list) and once with the search variant for sent mail (default second command: ['python3', '/home/ankit114/.claude/skills/gmail/read_inbox.py', 'search', '--query', 'in:sent newer_than:14d']) — override via --gmail-sent-cmd for tests. Sent messages map to direction 'outbound'; match the known person via the message's to/recipient fields when present, else via from_name/from_email heuristics on the payload the search returns (read the gmail JSON fields available: to field may be absent — when no recipient field exists, match sent messages to known people by scanning subject for the person's name as fallback, and count unmatchable sent mail in a 'sent_unmatched' receipt counter, never guessing). Sent-scan failure degrades with a note; inbox behavior unchanged.\n\nTESTS: at least 6 NEW 'def test_' functions across the two test files (keep all existing green; adjust only assertions that encoded the old inbox-only or candidate-authority behavior, noting each in a comment): stale cache >24h forces DEFICIT + flag; cache <=24h does not; candidate-inbox person NOT in HubSpot roster/ledger no longer becomes a touch (lands unclassified); sent-mail fixture marks a known person's thread answered (outbound touch registered, so no 'awaiting first response' nudge for them when outbound is newest); sent-scan failure degrades without affecting inbox collect; sent_unmatched counter increments for an unmatchable sent message.\n\nVERIFY: 'PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/test_hubspot_roster.py tests/test_funnel_evidence.py -q -p no:cacheprovider' green. You fail for touching files outside the owned list, fewer than 6 new tests, or the verify command failing.",
      "check": "python3 /mnt/d_drive/repos/loop-factory/ringer/one-true-master/checks/fix_with_tests.py --verify-command 'PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/test_hubspot_roster.py tests/test_funnel_evidence.py -q -p no:cacheprovider' --patch /home/ankit114/ringer-work/podcast-funnel-controller-r5/freshness.patch --owned-files 'scripts/hubspot_roster.py,scripts/funnel_evidence.py,tests/test_hubspot_roster.py,tests/test_funnel_evidence.py' --min-new-tests 6",
      "expect_files": [],
      "verified": "stale roster caches force conservative mode, the candidate inbox loses membership authority to the live HubSpot query, and sent mail registers outbound touches so answered threads stop showing as awaiting response"
    }
  ]
}
