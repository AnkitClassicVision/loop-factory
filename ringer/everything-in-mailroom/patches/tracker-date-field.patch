diff --git a/server/pipeline/referral_touch_tracker.py b/server/pipeline/referral_touch_tracker.py
index 1db5716..e6e59be 100644
--- a/server/pipeline/referral_touch_tracker.py
+++ b/server/pipeline/referral_touch_tracker.py
@@ -20,6 +20,7 @@ from datetime import date, datetime, timezone
 from email.utils import getaddresses, parseaddr
 from pathlib import Path
 from typing import Any, Iterable, Sequence
+from zoneinfo import ZoneInfo
 
 logger = logging.getLogger(__name__)
 
@@ -80,6 +81,12 @@ def _generated_at(value: str | datetime | None) -> str:
     return dt.astimezone(timezone.utc).isoformat()
 
 
+def _business_date(generated_at: str) -> str | None:
+    if generated_at == "invalid":
+        return None
+    return datetime.fromisoformat(generated_at).astimezone(ZoneInfo("America/New_York")).date().isoformat()
+
+
 def _normalize_email(value: str | None) -> str:
     raw = unicodedata.normalize("NFKC", str(value or ""))
     address = parseaddr(raw)[1] or raw
@@ -581,9 +588,11 @@ def build_referral_touch_report(
     else:
         aggregate_touch_status = "lower_bound"
     touch_coverage_complete = aggregate_touch_status == "complete"
+    normalized_generated_at = _generated_at(generated_at)
     return {
         "schema": SCHEMA,
-        "generated_at": _generated_at(generated_at),
+        "generated_at": normalized_generated_at,
+        "date": _business_date(normalized_generated_at),
         "mode": "read-only",
         "policy": "breadth once; no repeated asks without engagement",
         "subject_queries": [_safe_public_text(subject) for subject in effective_subjects],
diff --git a/tests/test_referral_touch_tracker.py b/tests/test_referral_touch_tracker.py
index 2fa3ce5..2cadddf 100644
--- a/tests/test_referral_touch_tracker.py
+++ b/tests/test_referral_touch_tracker.py
@@ -94,6 +94,27 @@ def row(report, name):
     return next(item for item in report["people"] if item["name"] == name)
 
 
+def test_report_date_matches_generated_at_business_date():
+    report = build([], [])
+
+    assert report["date"] == "2026-07-18"
+
+
+def test_report_date_uses_eastern_date_at_utc_day_boundary():
+    report = build_referral_touch_report(
+        contacts=[],
+        messages=[],
+        account_email=ACCOUNT,
+        subjects=DEFAULT_REFERRAL_SUBJECTS,
+        generated_at="2026-07-19T03:30:00+00:00",
+        source_receipt={"hubspot": "ok", "gmail": "ok"},
+        candidate_names=set(),
+    )
+
+    assert report["generated_at"].startswith("2026-07-19")
+    assert report["date"] == "2026-07-18"
+
+
 def test_never_asked_guests_are_ranked_newest_published_first():
     report = build(
         [
