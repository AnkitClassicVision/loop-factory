{
  "run_name": "everything-in-mailroom",
  "workdir": "/mnt/d_drive/ringer-work/everything-in-mailroom-fixes",
  "worktrees": true,
  "repo": "/mnt/d_drive/repos/podcast",
  "max_parallel": 2,
  "tasks": [
    {
      "key": "card-lane-all-loops",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 2400,
      "spec": "You are a repo fix worker. Your current working directory IS a git worktree of the OBE podcast repo (/mnt/d_drive/repos/podcast) checked out at HEAD — edit files here directly, relative to the worktree root.\n\nOWNERSHIP BOUNDARY: you own exactly two paths: scripts/run_podcast_loop.sh and tests/test_run_podcast_loop_card_lane.py (a new file you create). Never modify, create, or delete anything else. Never touch .git, never commit, never push, never run git add. Do not write notes or scratch files anywhere in the tree — the harness stages every changed file and FAILS the task if anything outside your two owned paths changed.\n\nCONTEXT: scripts/run_podcast_loop.sh delivers loop escalations to the owner's phone. Inside deliver_escalation_once() (around line 169) a case statement routes SOME loops through the Linear-card + phone-buttons lane (scripts/obe_escalation_card.py) with plain-text Telegram kept only as the delivery-outage fallback; loops NOT listed in the case fall straight to plain text the owner cannot act on. Owner decision (Ankit 2026-08-05): EVERY loop's escalation must go through the card lane; plain text remains only as the outage fallback.\n\nCHANGE: extend the case pattern in deliver_escalation_once() so all seven loops route to the card path: health, referral-flywheel, guest-acquisition, booking-readiness, production-publish, sales-handoff, proof-improvement. Preserve everything else exactly: the obe_escalation_card.py invocation and its arguments, the .delivered marker logic, the fallback-to-plain-telegram on card failure, and the existing log lines. Smallest possible edit; update the nearby owner-decision comment to note 2026-08-05: all loops carded.\n\nTESTS: create tests/test_run_podcast_loop_card_lane.py in the style of tests/test_tg_approval.py (plain pytest functions, no fixtures). It must read scripts/run_podcast_loop.sh from the repo root, extract the deliver_escalation_once() function body, and assert as separate test functions: (1) the card-lane case pattern contains all seven loop names above; (2) the obe_escalation_card.py invocation is still present in that branch; (3) the plain-text fallback path (the string 'falling back to plain telegram') is still present. This is a source-contract test pinning bash behavior, the same way test_tg_approval.py pins the button rows.\n\nHOW TO RUN (from the worktree root — all must pass): bash -n scripts/run_podcast_loop.sh && PLACEHOLDER_MODE=1 PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/test_run_podcast_loop_card_lane.py tests/test_tg_approval.py -q -p no:cacheprovider\n\ntests/test_tg_approval.py pins the LIVE email send lane's buttons; you must not modify it, and if your change breaks it you have touched something you must not.\n\nHARD RULES: no new dependencies, no rewrites, no reformatting of untouched lines, never invoke any network, model CLI, MCP, or skill, and leave your changes uncommitted — the harness exports the diff.",
      "check": "python3 /mnt/d_drive/repos/loop-factory/ringer/everything-in-mailroom/checks/fix_with_tests.py --verify-command 'bash -n scripts/run_podcast_loop.sh && PLACEHOLDER_MODE=1 PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/test_run_podcast_loop_card_lane.py tests/test_tg_approval.py -q -p no:cacheprovider' --patch /mnt/d_drive/repos/loop-factory/ringer/everything-in-mailroom/patches/card-lane-all-loops.patch --owned-files scripts/run_podcast_loop.sh,tests/test_run_podcast_loop_card_lane.py --min-new-tests 2 --require-test-file tests/test_run_podcast_loop_card_lane.py",
      "verified": "bash syntax check and both pytest files pass in the worktree, at least 2 new test functions exist in the new source-contract test file, only the two owned paths changed, and the diff was exported as a patch for coordinator review."
    },
    {
      "key": "blocked-draft-card",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 2400,
      "spec": "You are a repo fix worker. Your current working directory IS a git worktree of the OBE podcast repo (/mnt/d_drive/repos/podcast) checked out at HEAD — edit files here directly.\n\nOWNERSHIP BOUNDARY: you own exactly three paths: scripts/obe_tg_notify.py, scripts/obe_draft_to_linear_bridge.py, and tests/test_obe_tg_notify.py. Never modify anything else — in particular do NOT touch scripts/obe_escalation_card.py or tests/test_tg_approval.py. Never touch .git, never commit, never run git add; leave all changes uncommitted. No notes or scratch files — the harness stages every changed file and FAILS the task on anything outside your three owned paths.\n\nCONTEXT: the draft bridge (scripts/obe_draft_to_linear_bridge.py, runs headless every 30 minutes) QAs Gmail drafts; when a draft is BLOCKED it calls notify_blocked_once() in scripts/obe_tg_notify.py (around line 101), which sends a plain Telegram text, deduped per Gmail draft id via logs/tg-blocked-notified.json — one buzz per draft, not 48/day. Owner decision (Ankit 2026-08-05): every Telegram notification must be actionable — a card with buttons — never a scrolled-past text. The card lane already exists in the same file: notify_escalation() delivers a kind=escalation message with Approve/Respond buttons bound to a Linear card, action_class 'proposal'. INVARIANT (by construction, from the 2026-08-05 tg commit): only the --kind card email send lane may carry action_class external_send; a blocked-draft notice must NEVER be able to emit 'APPROVE external_send'. Your change must preserve that invariant.\n\nCHANGE: route the blocked-draft notification through the escalation-card lane. When the bridge knows a Linear card id for the blocked draft, deliver the notice as a kind=escalation buttons message bound to that card (Approve records a proposal approval on the card; Respond lands the owner's written instruction on the card). Modify the notify_blocked_once() signature and its call site in the bridge to pass the card id (or whatever minimal argument you need). KEEP: the per-draft-id dedup exactly as today; the plain-text path as fallback ONLY when no card id exists or the card lane fails (an alert must never be lost); and the never-fail-the-bridge contract — delivery failures land in logs/tg-notify-failures.jsonl and never raise into the bridge.\n\nTESTS: extend tests/test_obe_tg_notify.py following its existing stubbing style (it already fakes the ssh transport). Add test functions covering: (1) a blocked notice WITH a card id goes through the escalation lane as kind escalation / action_class proposal and never external_send; (2) dedup still suppresses a second notification for the same draft id; (3) a blocked notice WITHOUT a card id falls back to the plain-text path; (4) a card-lane failure falls back to plain text and never raises into the caller.\n\nHOW TO RUN (from the worktree root — all must pass): PLACEHOLDER_MODE=1 PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/test_obe_tg_notify.py -q -p no:cacheprovider && python3 -m py_compile scripts/obe_tg_notify.py scripts/obe_draft_to_linear_bridge.py\n\nHARD RULES: no new dependencies, minimal diff, no reformatting of untouched lines, tests must stub all transport (no network, no ssh, no model CLI, no MCP, no skills), and leave changes uncommitted — the harness exports the diff.",
      "check": "python3 /mnt/d_drive/repos/loop-factory/ringer/everything-in-mailroom/checks/fix_with_tests.py --verify-command 'PLACEHOLDER_MODE=1 PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/test_obe_tg_notify.py -q -p no:cacheprovider && python3 -m py_compile scripts/obe_tg_notify.py scripts/obe_draft_to_linear_bridge.py' --patch /mnt/d_drive/repos/loop-factory/ringer/everything-in-mailroom/patches/blocked-draft-card.patch --owned-files scripts/obe_tg_notify.py,scripts/obe_draft_to_linear_bridge.py,tests/test_obe_tg_notify.py --min-new-tests 3 --require-test-file tests/test_obe_tg_notify.py",
      "verified": "the obe_tg_notify test suite and py_compile pass in the worktree, at least 3 new test functions were added, only the three owned paths changed, and the diff was exported as a patch for coordinator review."
    }
  ]
}
