{
  "run_name": "everything-in-mailroom",
  "workdir": "/mnt/d_drive/ringer-work/everything-in-mailroom-r7-trackerdate",
  "worktrees": true,
  "repo": "/mnt/d_drive/repos/podcast",
  "max_parallel": 1,
  "tasks": [
    {
      "key": "tracker-date-field",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 1800,
      "spec": "You are a repo fix worker. Your current working directory IS a git worktree of the OBE podcast repo (/mnt/d_drive/repos/podcast) at HEAD — edit files here directly.\n\nOWNERSHIP BOUNDARY: you own exactly two paths: server/pipeline/referral_touch_tracker.py and tests/test_referral_touch_tracker.py. Nothing else — do NOT touch server/pipeline/referral_touch_automation.py (its gate is the consumer contract you are satisfying, read it read-only). Never touch .git, never commit, never run git add; leave changes uncommitted.\n\nCONTEXT — producer/consumer schema drift proven live 2026-08-05: the post-QA automation gate (server/pipeline/referral_touch_automation.py line ~131) requires the tracker report's top-level 'date' field to equal today's America/New_York business date, and blocks fail-closed otherwise. But the tracker writer (server/pipeline/referral_touch_tracker.py, payload built around line 586) emits NO 'date' field at all — today's real report has date: None, so the gate can never pass and the owner-approved Brandon Walley referral draft was blocked with reason \"tracker date is not today's America/New_York business date\".\n\nCHANGE (minimal): the tracker payload gains a top-level 'date' field: the America/New_York business date (YYYY-MM-DD) derived from the same moment used for generated_at — use zoneinfo.ZoneInfo('America/New_York') exactly like the automation gate does, so the two sides can never disagree on timezone. Respect the existing generated_at override parameter: when a caller supplies generated_at, 'date' derives from that supplied moment, not from wall-clock now. Do not change the schema constant, any other field, or any behavior.\n\nTESTS: extend tests/test_referral_touch_tracker.py in its existing style: (a) the payload contains 'date' equal to the ET business date of generated_at; (b) a generated_at late in the UTC evening that is still the previous day in ET yields the ET date, not the UTC date (the timezone edge that would silently re-break the gate); (c) existing tests still pass unchanged.\n\nHOW TO RUN (from the worktree root — all must pass): PLACEHOLDER_MODE=1 PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/test_referral_touch_tracker.py tests/test_referral_touch_automation.py tests/test_referral_touch_freshness.py -q -p no:cacheprovider && python3 -m py_compile server/pipeline/referral_touch_tracker.py\n\nHARD RULES: stdlib only, smallest diff, no reformatting, tests stub all transport, never invoke any network, model CLI, MCP, or skill, leave changes uncommitted — the harness exports the diff.",
      "check": "python3 /mnt/d_drive/repos/loop-factory/ringer/everything-in-mailroom/checks/fix_with_tests.py --verify-command 'PLACEHOLDER_MODE=1 PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/test_referral_touch_tracker.py tests/test_referral_touch_automation.py tests/test_referral_touch_freshness.py -q -p no:cacheprovider && python3 -m py_compile server/pipeline/referral_touch_tracker.py' --patch /mnt/d_drive/repos/loop-factory/ringer/everything-in-mailroom/patches/tracker-date-field.patch --owned-files server/pipeline/referral_touch_tracker.py,tests/test_referral_touch_tracker.py --min-new-tests 2 --require-test-file tests/test_referral_touch_tracker.py",
      "verified": "tracker emits the ET business date aligned with the automation gate's timezone math, the UTC-evening edge is pinned by test, all three referral suites pass, only the two owned paths changed, and the diff was exported for coordinator landing."
    }
  ]
}
