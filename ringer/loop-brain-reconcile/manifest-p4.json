{
  "run_name": "loop-brain-reconcile",
  "workdir": "/home/ankit114/ringer-work/loop-brain-reconcile-p4",
  "worktrees": true,
  "repo": "/mnt/d_drive/repos/loop-factory",
  "max_parallel": 2,
  "tasks": [
    {
      "key": "p4-lease-conductor-node",
      "engine": "codex",
      "task_type": "code-feature",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo — edit files here directly. Do NOT git commit/branch/push/stash; leave changes uncommitted (the check exports a patch). No MCP, no network, no skills; do not ask for approval — nobody is watching. NEVER read *.env/*secret*/*token*/*key*/*credential* files. YOU OWN EXACTLY: kernel/lease.py (create), factory/conductor.py (create), departments/podcast/runtime/conductor_tick.py (create), tests/test_lease.py (create), tests/test_conductor.py (create), departments/podcast/tests/test_conductor_tick.py (create). Modify nothing else. MISSION — plan Task 1 of docs/superpowers/plans/2026-08-05-p4-conductor-shadow.md (present in this worktree; its 'Frozen interfaces' section is the BINDING contract for every signature, file shape, decision kind, priority rule, and exit behavior — implement exactly, TDD order). SHADOW ABSOLUTE: the conductor never launches a process, never writes outside the state dir, never touches gateways or floors — its entire output is the decision ledger, the heartbeat, and lease files. Key implementation notes beyond the plan text: (1) kernel/lease.py — pure stdlib; acquire uses os.open O_CREAT|O_EXCL; expired takeover must be atomic (write new content to a temp file in the same dir then os.replace); LeaseHeld carries the current holder string; release verifies the nonce by re-reading the file before unlinking. (2) factory/conductor.py — every sense source wrapped so an unreadable/malformed file becomes an unknown_source decision, never a crash; for incidents.json and the approval queue, READ the real files under departments/podcast/state/ FIRST to match their actual field names (incident state values open/department_defect, approval rows with status pending_approval and queued_at — verify against reality and adapt field lookups, keeping the decision shapes from the plan); heals.jsonl matching: a fingerprint+playbook with a proposed row and no LATER row whose result is applied or verified; aging reads the previous 3 lines of conductor-shadow.jsonl. No PII: decisions carry fingerprints/node names/run ids/card identifiers only — never copy message bodies, names, or contact details out of sensed files. (3) conductor_tick.py — mirror the emit_record discipline of the other podcast runtime nodes (status ok unless the node crashed, emission failure fatal, trigger dedupe_key date-based); refused lease is a SUCCESSFUL tick (exit 0). TESTS: implement every test named in plan Task 1's checklist across the three new test files, including the two-acquirer arbitration proof and the mixed-fixture priority ordering. Watch each fail RED first. Green gate: PYTHONPATH=\"$PWD\" python3 -m pytest tests/test_lease.py tests/test_conductor.py departments/podcast/tests/test_conductor_tick.py -v.",
      "check": "bash /mnt/d_drive/repos/loop-factory/ringer/loop-brain-reconcile/checks/p4_check.sh p4-lease-conductor-node 'tests/test_lease.py tests/test_conductor.py departments/podcast/tests/test_conductor_tick.py' kernel/lease.py factory/conductor.py departments/podcast/runtime/conductor_tick.py tests/test_lease.py tests/test_conductor.py departments/podcast/tests/test_conductor_tick.py",
      "expect_files": [],
      "verified": "Lease proves exactly-one-driver with durable refusal receipts and nonce-guarded release; conductor tick senses verdicts/incidents/cards/heals/floors into prioritized would-dispatch decisions with aging, crash-proof unknown_source handling, heartbeat only on held ticks; node exits 0 on held or refused; all named tests green; patch touches only the six owned files."
    },
    {
      "key": "p4-deadman-wiring",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo — edit files here directly. Do NOT git commit/branch/push/stash; leave changes uncommitted (the check exports a patch). No MCP, no network, no skills; do not ask for approval — nobody is watching. NEVER read *.env/*secret*/*token*/*key*/*credential* files. YOU OWN EXACTLY: factory/estate_deadman.py, departments/podcast/runtime/podcast_daily.sh, departments/podcast/runtime/run-roster.json, tests/test_estate_deadman_conductor.py (create), departments/podcast/tests/test_daily_failclosed.py (extend, never delete tests). Modify nothing else — factory/conductor.py and runtime/conductor_tick.py do NOT exist in your worktree (another lane builds them); your edits are wiring and adoption-gated sensing, your tests use fixture heartbeat files, never the conductor itself. MISSION — plan Task 2 of docs/superpowers/plans/2026-08-05-p4-conductor-shadow.md (present in this worktree): (1) factory/estate_deadman.py — READ the whole file first; add an adoption-gated check mirroring the existing estate-manager staleness block's style exactly: for each registered department state dir containing conductor-heartbeat.json, parse its ts; older than 26 hours or unparseable => append the standard deadman escalation row (same outbox format the file already uses, message naming the department and 'conductor heartbeat stale'); no heartbeat file => no check, no escalation. (2) podcast_daily.sh — append EXACTLY this as the LAST node invocation line of the script, after the board regeneration commands: python3 \"${REPO}/factory/launch.py\" --department \"${DEPARTMENT}\" -- python3 \"${REPO}/departments/${DEPARTMENT}/runtime/conductor_tick.py\" --shadow --state-dir \"${STATE_DIR}\" --dept-dir \"${REPO}/departments/${DEPARTMENT}\" (3) run-roster.json — append node conductor_tick required true with the next ordinal (14; tail append, no renumbering). (4) tests/test_estate_deadman_conductor.py — fresh heartbeat no escalation; 27h-stale heartbeat escalates in the deadman's real outbox format (search tests/ for an existing estate_deadman test and copy its fixture style; if none exists build the minimal registry/state fixture the module's own main path reads — study estate_deadman's argument and file expectations); missing heartbeat file no check. (5) test_daily_failclosed.py — one static pin: conductor_tick.py is invoked and is the LAST runtime node invocation in the script. NOTE: departments/podcast/tests/test_run_roster.py will fail in YOUR worktree only on the required-source assertion for conductor_tick (its file is lane A's) — acceptable only for exactly that assertion; state it in your summary. bash -n must pass. Watch new tests RED first. Green gate: bash -n departments/podcast/runtime/podcast_daily.sh && PYTHONPATH=\"$PWD\" python3 -m pytest tests/test_estate_deadman_conductor.py departments/podcast/tests/test_daily_failclosed.py -v plus any existing estate_deadman test file you found (must not regress).",
      "check": "bash /mnt/d_drive/repos/loop-factory/ringer/loop-brain-reconcile/checks/p4_check.sh p4-deadman-wiring 'tests/test_estate_deadman_conductor.py departments/podcast/tests/test_daily_failclosed.py' factory/estate_deadman.py departments/podcast/runtime/podcast_daily.sh departments/podcast/runtime/run-roster.json tests/test_estate_deadman_conductor.py departments/podcast/tests/test_daily_failclosed.py",
      "expect_files": [],
      "verified": "Deadman watches conductor heartbeats adoption-gated in the module's own escalation format; conductor tick wired as the last chain node with roster ordinal 14; static pin + deadman tests green; patch touches only the five owned files."
    }
  ]
}
