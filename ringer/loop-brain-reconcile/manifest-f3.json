{
  "run_name": "loop-brain-reconcile",
  "workdir": "/home/ankit114/ringer-work/loop-brain-reconcile-f3",
  "worktrees": true,
  "repo": "/mnt/d_drive/repos/loop-factory",
  "max_parallel": 4,
  "tasks": [
    {
      "key": "f3-cards-v2",
      "engine": "codex",
      "task_type": "code-feature",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo — edit files here directly. Do NOT git commit/branch/push/stash; leave changes uncommitted (the check exports a patch). No MCP, no network, no skills; do not ask for approval — nobody is watching. NEVER read *.env/*secret*/*token*/*key*/*credential* files. YOU OWN EXACTLY: factory/human_in_the_loop.py, factory/manager.py, factory/estate_deadman.py, departments/podcast/runtime/escalate_outbox.py, factory/outbox_listen.py (ONLY if the podcast escalation schema validator lives there — find it by grepping for the string 'does not match podcast escalation schema' and own whichever file carries it), tests/test_card_format_v2.py (create), departments/podcast/tests/test_escalate_outbox_copy.py (extend, never delete tests). Modify nothing else. MISSION — section A of docs/superpowers/plans/2026-08-06-cards-v2-f3-sales.md (in this worktree; the frozen contract is binding; owner directive: every card carries make-sense + the EXACT approvable actions, or an explicit FYI marker; TDD order): (1) extend human_in_the_loop.escalate with the optional kwargs meaning/needs/actions/fyi_only exactly per the plan — action dicts {action, effect, reply}; rows gain the card block; calls supplying none of the new kwargs stamp card_gap true and prefix eli5 with '[unclear card] '; NEVER raise on old-style calls; keep the existing row fields untouched. (2) Upgrade the three producers per the plan: manager.py's escalate_fn wrapper in main() with the finding-code map (budget_telemetry_missing -> meaning 'Budget ceilings are set but no usage data exists, so spend cannot be verified' needs 'Wire the telemetry producer or confirm the path' actions [{action: 'Acknowledge until the P-next producer lands', effect: 'card stays parked, re-raised weekly', reply: 'approve ack-budget-telemetry'}]; budget_near:* -> action approve-pause-spend; runmanifest_red -> action approve-rerun-investigation; runmanifest_unverified, release_drift, backlog_aging, pace_under and unknown codes -> honest meaning from the detail string, needs 'Ops review', fyi_only true); estate_deadman.raise_alarm (meaning/needs + action inspect-restart with reply grammar); escalate_outbox.py threads each candidate's existing what_it_means/what_it_needs/one_question into the card block, ops-repair classes fyi_only true. (3) COMPATIBILITY IS THE HEART: grep the repo for 'does not match podcast escalation schema', READ that validator, extend it ADDITIVELY so rows with and without the card block BOTH validate and the block is never stripped; a malformed card block (wrong types) makes ONLY that row invalid, loudly. (4) TESTS in tests/test_card_format_v2.py: old-style escalate call -> card_gap true + '[unclear card] ' prefix; full v2 call -> card block exact shape; fyi_only call -> no approvable_actions key but fyi_only true; manager escalate_fn produces the budget_telemetry_missing card with its action + reply grammar; deadman alarm card; schema validator accepts old row, accepts new row with card, rejects a row whose card is a string. Extend test_escalate_outbox_copy.py: candidate meanings appear in the card block of emitted packets and packets still pass the schema validator. Watch each RED first. Green gate: PYTHONPATH=\"$PWD\" python3 -m pytest tests/test_card_format_v2.py departments/podcast/tests/test_escalate_outbox_copy.py tests/test_manager_hardening.py tests/test_estate_deadman_conductor.py -v.",
      "check": "bash /mnt/d_drive/repos/loop-factory/ringer/loop-brain-reconcile/checks/f3_check.sh f3-cards-v2 'tests/test_card_format_v2.py departments/podcast/tests/test_escalate_outbox_copy.py tests/test_manager_hardening.py tests/test_estate_deadman_conductor.py' factory/human_in_the_loop.py factory/manager.py factory/estate_deadman.py departments/podcast/runtime/escalate_outbox.py factory/outbox_listen.py tests/test_card_format_v2.py departments/podcast/tests/test_escalate_outbox_copy.py",
      "expect_files": [],
      "verified": "escalate carries meaning/needs/exact approvable actions or explicit FYI, gaps are loud, three producers upgraded, podcast packet schema extended additively with both-direction tests; all guard suites green; patch touches only owned files."
    },
    {
      "key": "f3-intake-qualify",
      "engine": "codex",
      "task_type": "code-feature",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo — edit files here directly. Do NOT git commit/branch/push/stash; leave changes uncommitted (the check exports a patch). No MCP, no network, no skills; do not ask for approval — nobody is watching. NEVER read *.env/*secret*/*token*/*key*/*credential* files (the fixture salt file you CREATE under a tests/fixtures dir with dummy content 'fixture-salt' is fine and expected). YOU OWN EXACTLY: departments/sales/runtime/intake_sensor.py (create), departments/sales/runtime/qualify_scorer.py (create), departments/sales/tests/__init__.py (create, empty), departments/sales/tests/test_intake_qualify.py (create). Modify nothing else. MISSION — section B nodes 1-2 of docs/superpowers/plans/2026-08-06-cards-v2-f3-sales.md (in this worktree; source shapes, subject_id hashing (sha256 of salt + lowercased stripped email, hex[:16], salt from <state>/sources/.id_salt, MISSING SALT = CRASH never a default), shared node conventions (argparse --state-dir --shadow, observations append, runrecord.emit_record always with date dedupe_key and fatal emission, exit 0 unless crashed) are all BINDING; study departments/podcast/runtime/manifest_sensor.py for the emit idiom and factory/events_ledger.py for append_event's exact signature and duplicate guard; TDD order): build intake_sensor.py (five lane files, lane dedup priority icaregrow > podcast_handoffs > pfs_warm > website_forms > luma, first lane wins, cross-lane same-day duplicates recorded in the observation metrics; NEW arrivals only — skip subjects whose received transition already exists in the ledger; missing lane files counted visibly, unparseable file = observation alarm) and qualify_scorer.py (bar by lane: pfs_* seller = role in {owner, decision_maker} AND exit_intent true, else services = role in {owner, decision_maker} AND icp_fit true; qualified appends received->qualified + qualifications.jsonl row {subject_id, bar, evidence, ts}; unqualified -> parked_out.jsonl with reason; already-qualified subjects skipped). PII FLOOR: nothing you write outside sources/ may contain an email or name — the events ledger enforces this and REJECTS emails; your tests must include one asserting a raw email never appears in events.jsonl, qualifications.jsonl, parked_out.jsonl, or observations.jsonl. TESTS (fixtures built inline in the test file or under departments/sales/tests/fixtures/ which you may create): intake happy path appends attributed received rows with opaque ids; re-run is idempotent (duplicate guard); cross-lane duplicate recorded with priority-lane win; missing lane file visible not fatal; missing salt crashes; qualify services + seller bars both ways (qualify and park-out); PII-absence test. Watch RED first. Green gate: PYTHONPATH=\"$PWD\" python3 -m pytest departments/sales/tests/test_intake_qualify.py -v.",
      "check": "bash /mnt/d_drive/repos/loop-factory/ringer/loop-brain-reconcile/checks/f3_check.sh f3-intake-qualify 'departments/sales/tests/test_intake_qualify.py' departments/sales/runtime/intake_sensor.py departments/sales/runtime/qualify_scorer.py departments/sales/tests/__init__.py departments/sales/tests/test_intake_qualify.py",
      "expect_files": [],
      "verified": "Intake dedups across lanes with priority + opaque ids + idempotent re-runs; qualifier applies the dual locked bars with evidence rows; PII-absence proven by test; all named tests green; patch touches only the four owned files."
    },
    {
      "key": "f3-calendar-receipts",
      "engine": "codex",
      "task_type": "code-feature",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo — edit files here directly. Do NOT git commit/branch/push/stash; leave changes uncommitted (the check exports a patch). No MCP, no network, no skills; do not ask for approval — nobody is watching. NEVER read *.env/*secret*/*token*/*key*/*credential* files (a fixture salt file with dummy content is fine). YOU OWN EXACTLY: departments/sales/runtime/booked_sensor.py (create), departments/sales/runtime/held_sensor.py (create), departments/sales/tests/test_calendar_receipts.py (create). Modify nothing else — departments/sales/tests/__init__.py is another lane's file; write your test to work whether or not it exists yet (use the same sys.path bootstrap idiom the podcast tests use if needed). MISSION — section B nodes 3-4 of docs/superpowers/plans/2026-08-06-cards-v2-f3-sales.md (in this worktree; BINDING: source shapes, hashing, node conventions, and the fast_path rule — a booking with no conversation_live transition appends qualified->booked with fast_path true in the receipt; held requires attended AND decision_maker_present AND minutes >= 20 AND start in the past, appends booked->held ONLY when both the bar (from qualifications.jsonl) and the source (from the received transition's meta) resolve — unresolvable attribution = observation alarm + NO transition, that is the DONE contract; study factory/events_ledger.py read/append APIs and the podcast emit idiom): build booked_sensor.py (calendar events matched by attendee-email hash to subjects with qualified or later; bookings.jsonl receipts {subject_id, event_id, start, fast_path, ts}; already-booked skipped) and held_sensor.py (held.jsonl receipts {subject_id, event_id, minutes, bar, source, ts}). TESTS: booked happy path + fast_path flag + idempotent re-run; no-show event produces NO held transition; short call (15 min) produces NO held; held happy path carries bar + source resolved from the actual ledger meta; attribution-unresolvable case alarms without a transition; PII-absence test over all written files. Watch RED first. Green gate: PYTHONPATH=\"$PWD\" python3 -m pytest departments/sales/tests/test_calendar_receipts.py -v.",
      "check": "bash /mnt/d_drive/repos/loop-factory/ringer/loop-brain-reconcile/checks/f3_check.sh f3-calendar-receipts 'departments/sales/tests/test_calendar_receipts.py' departments/sales/runtime/booked_sensor.py departments/sales/runtime/held_sensor.py departments/sales/tests/test_calendar_receipts.py",
      "expect_files": [],
      "verified": "Booked receipts with fast-path honesty; held receipts only with 20-min decision-maker attendance AND resolved bar + attribution (refusals alarm); idempotent; PII-absent; tests green; patch touches only the three owned files."
    },
    {
      "key": "f3-sense-gates",
      "engine": "codex",
      "task_type": "code-feature",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo — edit files here directly. Do NOT git commit/branch/push/stash; leave changes uncommitted (the check exports a patch). No MCP, no network, no skills; do not ask for approval — nobody is watching. NEVER read *.env/*secret*/*token*/*key*/*credential* files (fixture salt files with dummy content are fine). YOU OWN EXACTLY: departments/sales/runtime/sense_gates.py (create), departments/sales/tests/test_sense_gates.py (create). Modify nothing else; bootstrap sys.path in the test the way podcast tests do if the package __init__ is absent in your worktree. MISSION — section B node 5 of docs/superpowers/plans/2026-08-06-cards-v2-f3-sales.md (in this worktree; BINDING; CLI is --state-dir --dept-dir --shadow; observations sensor 'salesgate'; node conventions incl. runrecord emit; NO escalation calls in this phase — observations only, the coordinator wires cards at integration): implement the four context-is-king gates exactly: A staleness — subjects whose LATEST ledger transition is into conversation_live and whose threads.json row (matched by email hash) lacks a two-way (kind reply|live) within 7 days -> alarm observation with the count and subject ids; B double-touch — subjects with received transitions from 2+ distinct meta.source lanes within the charter's per_contact_cadence_floor_days (read via factory.charter_loader load_charter + the budget estate_send_caps section — READ the sales charter to get the exact key path) -> alarm with collision pairs; C context/voice coverage — drafts do not exist in v1: emit status ok with metrics {drafts: 0, checked: 0} (vacuous truth explicit); D floors attainment — read <dept-dir>/floors.yaml (absent -> status unknown observation, never a crash) and compare each stage's flow_per_week against THIS ISO week's ledger transition counts into that stage -> one alarm observation listing per-stage shortfalls when any stage is short, ok otherwise. TESTS: stale conversation alarms with the right subject; fresh conversation does not; cross-lane collision inside the cadence window alarms, outside does not; missing floors.yaml -> unknown; short week -> alarm with the exact shortfall numbers; healthy week -> ok; emit_record always called; PII-absence over observations. Watch RED first. Green gate: PYTHONPATH=\"$PWD\" python3 -m pytest departments/sales/tests/test_sense_gates.py -v.",
      "check": "bash /mnt/d_drive/repos/loop-factory/ringer/loop-brain-reconcile/checks/f3_check.sh f3-sense-gates 'departments/sales/tests/test_sense_gates.py' departments/sales/runtime/sense_gates.py departments/sales/tests/test_sense_gates.py",
      "expect_files": [],
      "verified": "Four gates implemented exactly (staleness truth, cross-lane suppression, explicit vacuous voice coverage, floors attainment with shortfall math); fail-visible unknowns; PII-absent; tests green; patch touches only the two owned files."
    },
    {
      "key": "f3-shell-roster-fixtures",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo — edit files here directly. Do NOT git commit/branch/push/stash; leave changes uncommitted (the check exports a patch). No MCP, no network, no skills; do not ask for approval — nobody is watching. NEVER read *.env/*secret*/*token*/*key*/*credential* files. YOU OWN EXACTLY: departments/sales/runtime/sales_daily.sh (REPLACE the scaffold), departments/sales/runtime/run-roster.json (create), departments/sales/tests/test_daily_static.py (create), departments/sales/tests/fixtures/sources/ (create the directory and its files). Modify nothing else — the runtime node .py files do NOT exist in your worktree (other lanes build them); your shell references them as text and your tests are static (grep/parse/bash -n), never executing nodes. MISSION — section C of docs/superpowers/plans/2026-08-06-cards-v2-f3-sales.md (in this worktree; BINDING): (1) REPLACE sales_daily.sh with the podcast-pattern chain — READ departments/podcast/runtime/podcast_daily.sh and copy its idioms exactly (set -euo pipefail, REPO/DEPARTMENT/STATE_DIR/SOURCES/QUEUE/OUTBOX vars with SALES paths, json_object_field helper, mkdir, kernel.run_manifest mint block with LOOP_FACTORY_RUN_ID export, launch.py confinement per node, ver_rc verify block, manager --budget line, human_in_the_loop push line, conductor_tick last). Chain order: mint -> intake_sensor -> qualify_scorer -> booked_sensor -> held_sensor -> sense_gates (--dept-dir arg) -> floor_compiler_run (--dept-dir arg) -> verify -> manager --budget -> push -> conductor_tick. NOTE floor_compiler_run and conductor_tick live in departments/podcast/runtime/ — sales v1 REUSES them? NO: they are department-generic wrappers but live under podcast; instead invoke them via their podcast paths ONLY if they take --state-dir/--dept-dir generically — READ both files; they hardcode department='podcast' in emit_record, so DO NOT reuse: OMIT floor_compiler_run and conductor_tick from the sales chain in this phase and note in a comment that sales floor compilation + conductor adoption arrive when the wrappers are factored department-generic (a real finding — record it prominently in your final summary). Chain therefore: mint -> intake -> qualify -> booked -> held -> sense_gates -> verify -> manager --budget -> push. (2) run-roster.json: ordinals for exactly the invoked runtime nodes in order (intake_sensor 1, qualify_scorer 2, booked_sensor 3, held_sensor 4, sense_gates 5), all required true (all pre-verify). (3) Fixtures under departments/sales/tests/fixtures/sources/ exactly per plan section C (synthetic example.test emails; the .id_salt file content 'fixture-salt'). (4) test_daily_static.py: bash -n passes; the chain order above pinned by index comparisons; mint before the first node and verify before manager; roster consistency test (whole-script scan; every roster node invoked; every invoked runtime node rostered; all roster nodes required true — none run post-verify in this chain). Watch RED first. Green gate: bash -n departments/sales/runtime/sales_daily.sh && PYTHONPATH=\"$PWD\" python3 -m pytest departments/sales/tests/test_daily_static.py -v.",
      "check": "bash /mnt/d_drive/repos/loop-factory/ringer/loop-brain-reconcile/checks/f3_check.sh f3-shell-roster-fixtures 'departments/sales/tests/test_daily_static.py' departments/sales/runtime/sales_daily.sh departments/sales/runtime/run-roster.json departments/sales/tests/test_daily_static.py departments/sales/tests/fixtures/sources/icaregrow.json departments/sales/tests/fixtures/sources/luma.json departments/sales/tests/fixtures/sources/website_forms.json departments/sales/tests/fixtures/sources/podcast_handoffs.json departments/sales/tests/fixtures/sources/pfs_warm.json departments/sales/tests/fixtures/sources/calendar_events.json departments/sales/tests/fixtures/sources/threads.json departments/sales/tests/fixtures/sources/.id_salt",
      "expect_files": [],
      "verified": "Sales daily chain wired podcast-pattern with mint/verify/manager/push, roster consistent and fully pre-verify, synthetic fixtures complete; static pins green; patch touches only owned files; generic-wrapper finding reported."
    }
  ]
}
