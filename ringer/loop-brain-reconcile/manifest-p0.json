{
  "run_name": "loop-brain-reconcile",
  "workdir": "/home/ankit114/ringer-work/loop-brain-reconcile-p0",
  "worktrees": true,
  "repo": "/mnt/d_drive/repos/loop-factory",
  "max_parallel": 4,
  "tasks": [
    {
      "key": "p0-expectation-shell",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo — edit files here directly. Do NOT git commit/branch/push/stash; leave all changes uncommitted (the check exports a patch). No MCP, no network, no skills; do not ask for approval — nobody is watching. NEVER read *.env/*secret*/*token*/*key*/*credential* files. YOU OWN EXACTLY these files and may modify/create no others: departments/podcast/runtime/expectation_reconcile.py, departments/podcast/runtime/podcast_daily.sh, departments/podcast/tests/test_expectation_exitcodes.py (create), departments/podcast/tests/test_daily_failclosed.py (extend — never delete existing tests). MISSION (plan Tasks 1+3 of docs/superpowers/plans/2026-08-05-p0-close-silent-skips.md, which is present in this worktree — its Task 1 and Task 3 sections contain the EXACT test code and shell block to use; follow them verbatim, TDD order: write tests, watch them fail, implement, watch them pass): (1) expectation_reconcile.py main() currently ends with 'return 0 if worst == \"ok\" and not errors else 1'. Split the contract: crashes (errors non-empty) return 1; findings verdicts (worst != ok, no errors) return 2; all-ok returns 0. Add the plan's comment explaining the contract. (2) podcast_daily.sh line 127 currently silences the sensor with '|| true'. Replace that single line with the plan Task 3 Step 3 block: capture exp_rc, treat exit 2 as a valid findings verdict and continue, any other nonzero exit aborts the chain with a message to stderr (mirror the existing dag_supervisor sup_rc pattern at lines 140-146). ALSO in podcast_daily.sh, per plan Task 6 Step 4: extend line 152 (the factory/manager.py invocation) with --budget \"${STATE_DIR}/budget_used.json\" (this file is shell-owned by YOU because file ownership is disjoint across concurrent lanes; the manager-side --budget flag is another lane's work — do NOT touch factory/manager.py). (3) Create test_expectation_exitcodes.py with plan Task 1 Step 1's exact content. (4) Append plan Task 3 Step 1's test_expectation_line_has_no_silent_bypass to test_daily_failclosed.py. Note the daily-script grep test must pass with your edited script. Run: PYTHONPATH=\"$PWD\" python3 -m pytest departments/podcast/tests/test_expectation_exitcodes.py departments/podcast/tests/test_daily_failclosed.py -v and bash -n departments/podcast/runtime/podcast_daily.sh until green.",
      "check": "bash /mnt/d_drive/repos/loop-factory/ringer/loop-brain-reconcile/checks/p0_check.sh p0-expectation-shell 'departments/podcast/tests/test_expectation_exitcodes.py departments/podcast/tests/test_daily_failclosed.py' departments/podcast/runtime/expectation_reconcile.py departments/podcast/runtime/podcast_daily.sh departments/podcast/tests/test_expectation_exitcodes.py departments/podcast/tests/test_daily_failclosed.py",
      "expect_files": [],
      "verified": "Exit contract 0/2/1 implemented, || true replaced with the alarm-verdict pattern, --budget wired into the manager line; both test files green in the worktree; patch exported touching only the four owned files."
    },
    {
      "key": "p0-compare-expectation",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo — edit files here directly. Do NOT git commit/branch/push/stash; leave all changes uncommitted (the check exports a patch). No MCP, no network, no skills; do not ask for approval — nobody is watching. NEVER read *.env/*secret*/*token*/*key*/*credential* files. YOU OWN EXACTLY these files and may modify/create no others: departments/podcast/runtime/compare_charter.py, departments/podcast/tests/test_compare_charter.py (extend — never delete existing tests). MISSION (plan Task 2 of docs/superpowers/plans/2026-08-05-p0-close-silent-skips.md, present in this worktree — its Task 2 section has the exact entries; TDD order): compare_charter.py's FAILURE_CLASSES table (line ~27) has no ('expectation', ...) entries, so any observation from the expectation_reconcile sensor makes compare_observations raise ValueError('no charter comparison transition...'). Add after the ('manifest','unknown') entry: ('expectation','unknown') -> ('expectation_blind','high') and ('expectation','alarm') -> ('expectation_delta','high'), with the plan's comment. Add the two MEANINGS entries (expectation_blind, expectation_delta) with the plan's exact what_it_means / what_it_needs owner-language wording, and the QUESTIONS['expectation'] entry. Extend test_compare_charter.py with plan Task 2 Step 1's two tests — first READ the existing file to see how its current tests construct the charter argument and adapt the fixture usage to match (the plan says: copy the existing construction verbatim; there may be no minimal_charter fixture — build whatever the neighboring tests build). Run: PYTHONPATH=\"$PWD\" python3 -m pytest departments/podcast/tests/test_compare_charter.py -v until fully green (old and new tests).",
      "check": "bash /mnt/d_drive/repos/loop-factory/ringer/loop-brain-reconcile/checks/p0_check.sh p0-compare-expectation 'departments/podcast/tests/test_compare_charter.py' departments/podcast/runtime/compare_charter.py departments/podcast/tests/test_compare_charter.py",
      "expect_files": [],
      "verified": "Expectation transitions (blind/delta, both high) added with owner-language MEANINGS and QUESTIONS; full compare test file green; patch touches only the two owned files."
    },
    {
      "key": "p0-manager-budget",
      "engine": "codex",
      "task_type": "code-feature",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo — edit files here directly. Do NOT git commit/branch/push/stash; leave all changes uncommitted (the check exports a patch). No MCP, no network, no skills; do not ask for approval — nobody is watching. NEVER read *.env/*secret*/*token*/*key*/*credential* files. YOU OWN EXACTLY these files and may modify/create no others: factory/manager.py, templates/department_daily.sh.template, tests/test_manager_budget_failclosed.py (create). Do NOT touch departments/podcast/runtime/podcast_daily.sh — another lane owns it. MISSION (plan Tasks 4+5+6 of docs/superpowers/plans/2026-08-05-p0-close-silent-skips.md, present in this worktree — those sections contain the exact code; TDD order): (1) In factory/manager.py sense() (budget block at lines ~189-198), distinguish three states per the plan Task 4 Step 3 code: budget_unconfigured (budget_path is None), budget_missing (path given, file absent), budget_unreadable (file present, corrupt — existing behavior). Add 'budget_telemetry_missing' and 'budget_telemetry_unconfigured' booleans to the returned snapshot next to the existing budget keys. (2) In compare() directly after the existing budget_telemetry_unreadable finding (~line 454), add plan Task 5 Step 3's two findings: budget_telemetry_missing severity 'breach' and budget_telemetry_unconfigured severity 'warn', both gated on t['budget_ceilings'] being non-empty, using the existing _finding(code, severity, detail, observed=None, setpoint=None) helper. (3) In main()'s argparse (after --outbox, ~line 978) add the plan Task 6 Step 3 --budget argument, and extend the run_manager_cycle call (~line 1022) with budget_path=args.budget (run_manager_cycle forwards unknown kwargs to sense via **telemetry_paths — do not change its signature). (4) In templates/department_daily.sh.template line 25, append --budget \"${STATE_DIR}/budget_used.json\" to the manager.py invocation. (5) Create tests/test_manager_budget_failclosed.py with ALL test code from plan Tasks 4, 5, and 6 Step 1 (seven tests total: missing flagged, unconfigured not missing, present loads, corrupt unreadable, missing breaches, unconfigured warns, no-ceilings silent, plus run_manager_cycle forwards). Run: PYTHONPATH=\"$PWD\" python3 -m pytest tests/test_manager_budget_failclosed.py tests/test_manager_hardening.py -v until fully green — the hardening file guards against regressions and you must not modify it.",
      "check": "bash /mnt/d_drive/repos/loop-factory/ringer/loop-brain-reconcile/checks/p0_check.sh p0-manager-budget 'tests/test_manager_budget_failclosed.py tests/test_manager_hardening.py' factory/manager.py templates/department_daily.sh.template tests/test_manager_budget_failclosed.py",
      "expect_files": [],
      "verified": "sense() reports missing/unconfigured/unreadable budget states, compare() breaches on missing and warns on unconfigured, --budget CLI wired through run_manager_cycle, template updated; new tests plus manager hardening green; patch touches only the three owned files."
    },
    {
      "key": "p0-emit-regression-pin",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo — edit files here directly. Do NOT git commit/branch/push/stash; leave all changes uncommitted (the check exports a patch). No MCP, no network, no skills; do not ask for approval — nobody is watching. NEVER read *.env/*secret*/*token*/*key*/*credential* files. YOU OWN EXACTLY one file: departments/podcast/tests/test_records_integrity.py (extend — never delete existing tests). You may READ anything else but modify NOTHING else. MISSION (plan Task 7 of docs/superpowers/plans/2026-08-05-p0-close-silent-skips.md, present in this worktree): pin as a regression test that a runs-v2 emit failure FAILS the node (runbook rule: a step without its receipt cannot advance). Append plan Task 7 Step 1's test_manifest_sensor_emit_failure_propagates: monkeypatch manifest_sensor._run to return [] and manifest_sensor.runrecord.emit_record to raise OSError('simulated runs-v2 append failure'), then assert manifest_sensor.run(tmp_path, tmp_path) raises that OSError. FIRST read test_records_integrity.py's existing imports — import departments.podcast.runtime.manifest_sensor the same way that file (or its neighbors in the same directory) imports runtime modules; adapt only the import mechanism, keep the test body from the plan. EXPECTED: the test passes IMMEDIATELY (current code already re-raises — this pins it). If it FAILS against current code, STOP: do not modify manifest_sensor.py (you do not own it); instead write the failing output verbatim into a comment at the end of your test (marked '# BLOCKED: propagation broken —') and leave the test in a state that documents reality via pytest.mark.xfail with the reason string, so the orchestrator investigates. Run: PYTHONPATH=\"$PWD\" python3 -m pytest departments/podcast/tests/test_records_integrity.py -v until green.",
      "check": "bash /mnt/d_drive/repos/loop-factory/ringer/loop-brain-reconcile/checks/p0_check.sh p0-emit-regression-pin 'departments/podcast/tests/test_records_integrity.py' departments/podcast/tests/test_records_integrity.py",
      "expect_files": [],
      "verified": "Emit-failure propagation pinned as an executable regression test; records-integrity file fully green; patch touches only that one test file."
    }
  ]
}
