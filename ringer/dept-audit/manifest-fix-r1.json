{
  "run_name": "department-audit",
  "workdir": "/home/ankit114/ringer-work/dept-fix-r1",
  "worktrees": true,
  "repo": "/mnt/d_drive/repos/loop-factory",
  "max_parallel": 2,
  "tasks": [
    {
      "key": "w1-podcast-failclosed-maps",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo. Do NOT git commit/branch/push/stash. No MCP, no network, no skills; do not ask for approval — nobody is watching; fixture-only tests. NEVER read *.env/*secret*/*token*/*key*/*credential* files. NEVER edit charter.yaml or anything under runbooks/. TWO CONFIRMED AUDIT FINDINGS to fix in the podcast department (read the full audit first: ringer/dept-audit/reports/podcast-review.md). FINDING 1 — heal failures suppressed: departments/podcast/runtime/podcast_daily.sh lines ~52-70 run heal_select/heal_apply/heal_verify and the incident-list load with `|| true`, so a CRASH (as opposed to a recorded refusal) in any of them vanishes: no receipt, no manager-visible record, and the chain advances. FIX CONTRACT (preserves the original design intent that one bad incident never kills the whole daily run): per incident fingerprint, if select/parse/apply/verify exits nonzero or emits unparseable output, HALT that incident's lane immediately (skip its remaining heal steps), append ONE manager-visible failure receipt for that fingerprint into the department state (a small JSON line in a heal_failures.jsonl or equivalent the manager can sense — look at how heal_verify.py records status and match its shape/conventions), and continue with OTHER incidents; the incident-list load itself failing must abort the heal phase with nonzero, not silently produce an empty list. FINDING 2 — map-runtime dishonesty, 3/3 spot checks mismatched: (a) procedural-graph.md ~28-42 places N6 alongside N1 before compare, but podcast_daily.sh ~21-44 never runs N6 there; (b) subgraphs.json ~223-230 says rotation is not daily, but podcast_daily.sh ~72-73 executes it every daily run; (c) SG-PIPELINE in subgraphs.json ~98-114 declares N1->N2->N9 but the shell runs N1, then other sensors, then one shared compare, and never invokes record.py as that lane's N9. DECISION RULE: the running shell reflects operating reality proven in shadow — make the MAPS honest to execution, EXCEPT where execution contradicts an explicit charter statement (then fix the shell and say so in evidence.md). Update procedural-graph.md AND subgraphs.json together; `python3 loopfactory.py validate --name podcast` must PASS after. TESTS: departments/podcast/tests/test_daily_failclosed.py, >=5 fixture tests exercising the orchestrator's heal phase in a tmp state dir (drive the loop body via bash with stubbed heal scripts on PATH or extracted shell functions — your design, but tests must EXECUTE the real shell logic, not a python reimplementation): crash in heal_select records a failure receipt and skips that incident's apply/verify; crash in heal_apply records and skips verify; other incidents still processed after one fails; incident-load failure aborts nonzero; happy path unchanged (refusal receipts still work, no failure receipt written). Existing suite: PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/podcast/tests -q must stay green. Write ./evidence.md (per-finding: what changed, which direction the map/shell decision went and why, test output). VERIFY: PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/podcast/tests -q -p no:cacheprovider && python3 loopfactory.py validate --name podcast.",
      "check": "python3 /mnt/d_drive/repos/loop-factory/ringer/one-true-master/checks/fix_with_tests.py --verify-command 'PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/podcast/tests -q -p no:cacheprovider && python3 loopfactory.py validate --name podcast' --patch /home/ankit114/ringer-work/dept-fix-r1/podcast-w1.patch --owned-files 'departments/podcast/runtime/podcast_daily.sh,departments/podcast/procedural-graph.md,departments/podcast/subgraphs.json,departments/podcast/tests,evidence.md' --min-new-tests 5",
      "expect_files": [],
      "verified": "podcast heal phase fails closed per incident with manager-visible failure receipts, and the maps are an honest executable description again — validate PASS"
    },
    {
      "key": "w1-social-receipt-chain",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo. Do NOT git commit/branch/push/stash. No MCP, no network, no skills; do not ask for approval — nobody is watching; fixture-only tests, no live services, no real tokens. NEVER read *.env/*secret*/*token*/*key*/*credential* files. NEVER edit charter.yaml, subgraphs.json, or anything under runbooks/. ONE CONFIRMED AUDIT FINDING to fix in the social department (read ringer/dept-audit/reports/social-review.md, dimension 5): departments/social/runtime/social_daily.sh has two escapes from its own receipt chain — (a) around lines ~491-515 a weekly-cap YIELD exits the run before delivery verification and the N9 record, so a valid yield leaves NO terminal run record in canonical history; (b) around lines ~517-521 the N10 review-card creation runs OUTSIDE run_step and explicitly ignores failure, so the outbound human gate can fail without summoning the manager. FIX CONTRACT (smallest change, do NOT restructure the guard chain): (a) before the cap-yield exit, append a terminal run record with a yielded/skipped status through the SAME record path the other stages use (read how run_step and the N9 record stage do it at lines ~66-148 and follow those conventions exactly); (b) route the review-card creation through run_step (or equivalent receipt-gated invocation) so a failure produces a blocked/failed receipt and nonzero propagation the manager can sense — the card CREATION failing must not be silently ignored; a successful run must behave byte-for-byte as before aside from the new receipt/record. TESTS: departments/social/tests/test_receipt_chain.py, >=5 fixture tests that EXECUTE the real shell logic with stubbed commands on PATH in a tmp state dir: cap-yield path writes the terminal yielded record; yield record carries the same schema/shape as other stage records; review-card failure produces a failed receipt and nonzero (not silence); review-card success produces its receipt and the run continues; full happy-path run unchanged (same receipts as before plus nothing spurious). Existing suite: PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/social/tests -q must stay green. Write ./evidence.md (what changed at which lines, test output). VERIFY: PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/social/tests -q -p no:cacheprovider && python3 loopfactory.py validate --name social.",
      "check": "python3 /mnt/d_drive/repos/loop-factory/ringer/one-true-master/checks/fix_with_tests.py --verify-command 'PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/social/tests -q -p no:cacheprovider && python3 loopfactory.py validate --name social' --patch /home/ankit114/ringer-work/dept-fix-r1/social-w1.patch --owned-files 'departments/social/runtime/social_daily.sh,departments/social/tests/test_receipt_chain.py,evidence.md' --min-new-tests 5",
      "expect_files": [],
      "verified": "social's cap-yield and review-card stages are inside the receipt chain — every terminal state leaves a record, no silent outbound-gate failure"
    },
    {
      "key": "w1-kernel-nonce-strict",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 3600,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo. Do NOT git commit/branch/push/stash. No MCP, no network, no skills; do not ask for approval — nobody is watching; fixture-only tests. NEVER read *.env/*secret*/*token*/*key*/*credential* files. THIS IS SHARED KERNEL CODE on the deny-by-default publish path — the test burden is the point; behavior for well-formed ledgers must be byte-identical. ONE CONFIRMED AUDIT FINDING (read ringer/dept-audit/reports/social-review.md, dimension 4): kernel/lock_service.py lines ~58-70 catch malformed consumed/revoked nonce-ledger rows and continue, with no distinction between (a) a torn TRAILING line from an interrupted append — an expected crash artifact — and (b) corruption of an EARLIER durable row, which silently erases replay evidence on the highest-blast-radius path. FIX CONTRACT: tolerate at most ONE provably-torn FINAL line (define torn precisely: the last physical line, failing to parse, with no newline terminator OR truncated JSON — document your definition in a comment); ANY malformed non-final row (or a malformed final row that does not meet the torn definition) must REFUSE startup of the lock service with a clear error naming the file and line number, consistent with LockServiceDown deny-by-default semantics (a gateway that errors REFUSES — see kernel conventions in the file header and factory-pipeline hard rules). Read every call site of the loading path first (grep for the loader across kernel/ and departments/) and keep the public interface unchanged. TESTS: extend tests/test_kernel_hardening.py (extend, never delete existing tests), >=4 new: torn final line (no trailing newline, truncated JSON) tolerated and logged, service starts; malformed middle row refuses startup naming file+line; malformed final row that IS complete JSON-garbage-with-newline refuses (not torn); clean ledger byte-identical behavior (round-trip a consume and reload). Existing kernel-related suites must stay green. Write ./evidence.md (torn-line definition, call sites checked, test output). VERIFY: PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/test_kernel_hardening.py tests/test_review_hardening.py tests/test_telemetry_rollup.py -q -p no:cacheprovider.",
      "check": "python3 /mnt/d_drive/repos/loop-factory/ringer/one-true-master/checks/fix_with_tests.py --verify-command 'PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/test_kernel_hardening.py tests/test_review_hardening.py tests/test_telemetry_rollup.py -q -p no:cacheprovider' --patch /home/ankit114/ringer-work/dept-fix-r1/kernel-w1.patch --owned-files 'kernel/lock_service.py,tests/test_kernel_hardening.py,evidence.md' --min-new-tests 4",
      "expect_files": [],
      "verified": "nonce ledger tolerates exactly one provably-torn final line and refuses startup on any other corruption — replay evidence can no longer silently vanish"
    },
    {
      "key": "w1-cadence-contracts",
      "engine": "codex",
      "task_type": "code-feature",
      "timeout_s": 1800,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo. Do NOT git commit/branch/push/stash. No MCP, no network, no skills; do not ask for approval — nobody is watching. NEVER edit charter.yaml, runtime code, or anything under runbooks/. NEVER invoke systemctl. MISSION: author the missing Stage 8 cadence/alert contracts for the podcast and social departments — both audits flagged 'no cadence contract exists' (P1). READ FIRST: factory/cadence.py (the validator — your output must pass it), tests/fixtures/cadence/valid.yaml (the canonical shape), departments/pulse — deleted, so instead use ringer/ultimate-process/acceptance-pulse/RECEIPTS.md Stage 8 row for precedent; then derive each department's REAL cadence from what exists today: departments/podcast/runtime/ (find its systemd units/timers and daily trigger — the dept runs a ~30min sense cycle per charter setpoints.operational.sense_cycle_minutes and a daily chain), departments/social/runtime/systemd/ (the manager service/timer files state their schedule), departments/social/charter.yaml escalation section, departments/podcast/charter.yaml escalation section (owner, ping ceiling -> digest cap). WRITE: departments/podcast/creation/cadence.yaml and departments/social/creation/cadence.yaml, each fully passing python3 loopfactory.py cadence check --contract <path> --render-dir /tmp/cad-<dept>: triggers derived from the ACTUAL observed schedule (document the source of each value in a YAML comment — no invented numbers; where no evidence exists for a value, choose the factory-precedent default and mark it '# DEFAULT pending owner review'); classification_table covering at least objective_breach->human, release_drift->proposal, plus any incident codes you find in that department's runtime (grep for escalation/incident codes); digest caps derived from the charter's ping ceiling where declared; escalation owner ankit; activation.enabled_by_default MUST be false. Write ./evidence.md: per department a table value -> source (file:line or 'DEFAULT pending owner review'), plus both cadence-check outputs. VERIFY: python3 loopfactory.py cadence check --contract departments/podcast/creation/cadence.yaml --render-dir /tmp/cadp && python3 loopfactory.py cadence check --contract departments/social/creation/cadence.yaml --render-dir /tmp/cads.",
      "check": "python3 /mnt/d_drive/repos/loop-factory/ringer/one-true-master/checks/fix_with_tests.py --verify-command 'python3 loopfactory.py cadence check --contract departments/podcast/creation/cadence.yaml --render-dir /tmp/cadp && python3 loopfactory.py cadence check --contract departments/social/creation/cadence.yaml --render-dir /tmp/cads' --patch /home/ankit114/ringer-work/dept-fix-r1/cadence-w1.patch --owned-files 'departments/podcast/creation/cadence.yaml,departments/social/creation/cadence.yaml,evidence.md' --min-new-tests 0",
      "expect_files": [],
      "verified": "both departments carry Stage 8 cadence contracts derived from observed schedules with sourced values, passing the cadence gate, never enabled by default"
    }
  ]
}
