{
  "run_name": "department-audit",
  "workdir": "/home/ankit114/ringer-work/dept-fix-r3b",
  "worktrees": true,
  "repo": "/mnt/d_drive/repos/loop-factory",
  "max_parallel": 1,
  "tasks": [
    {
      "key": "w3-podcast-sensors-v2",
      "engine": "codex",
      "task_type": "code-feature",
      "timeout_s": 4800,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo. Do NOT git commit/branch/push/stash. No MCP, no network, no skills; do not ask for approval — nobody is watching; fixture-only tests, no live services. NEVER read *.env/*secret*/*token*/*key*/*credential* files. NEVER edit charter.yaml or anything under runbooks/. A PRIOR ATTEMPT AT THIS TASK WAS REJECTED at review because it REWROTE the pinned publish_reliability computation in departments/podcast/runtime/hopper_sensor.py — the repo-root suite tests/test_hopper_sensor.py pins that function's exact semantics (publish_schedule.json due-episodes + same-day publishday verifier rows in observations.jsonl), plus the observed-document shape, foreign-value preservation, and honest-absent behavior. THE PIN WINS. HARD CONSTRAINT: do not change the existing _publish_reliability logic, the hopper_depth logic, the document-merge/atomic-write behavior, or any existing function signature in hopper_sensor.py; tests/test_hopper_sensor.py must pass UNMODIFIED — run it early and often. MISSION: ADD sensing for the three objectives the gate reports absent — hopper_interviews_ready, state_drift, unledgered_inbound — as PURE ADDITIONS: (1) declare their evidence paths as configuration following the existing house pattern (departments/podcast/runtime/estate.json precedent — add an objectives_evidence block mapping objective name -> absolute path/glob); at runtime read what the config points at; a missing/unreadable/malformed source leaves that value ABSENT (omitted from values) with a logged reason — NEVER 0, never invented. (2) The real evidence lives in the podcast repo (/mnt/d_drive/repos/podcast — READ-ONLY, never write there): read its FUNNEL-LEDGER.json and reconcile receipts to learn the actual shapes; hopper_interviews_ready = finished interviews ready in the hopper per the ledger's real shape; state_drift and unledgered_inbound ONLY if reconcile artifacts expose them directly — if the shape does not exist yet, leave the value ABSENT and record exactly which artifact is missing in evidence.md. (3) Extend OWNED_VALUES and the additive computation path so the new values merge into the SAME objectives_observed.json document the existing code writes, preserving every existing behavior (foreign values, statuses, absent semantics). (4) If (and only if) the wiring needs it, minimal additive edits to podcast_daily.sh / subgraphs.json / procedural-graph.md — every pinned assertion in tests/test_heal_chain_wiring.py, tests/test_rotate_observations.py, tests/test_comms_reconcile_sensor.py must stay green untouched. python3 loopfactory.py validate --name podcast must PASS. TESTS: departments/podcast/tests/test_objectives_sensors.py, >=7 fixture tests (config pointing at tmp fixture files, never real cross-repo paths): each new objective computed correctly from a fixture; absent-when-evidence-missing per objective (omitted, not 0); existing hopper_depth and publish_reliability behavior UNCHANGED with the new config present; malformed evidence file -> absent with reason, exit clean; a breach case accepted by python3 -m factory.objectives_verify fixture invocation (exit 1 with WHY). Write ./evidence.md (evidence shapes found in the podcast repo with exact paths, what stays absent and why). VERIFY: PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/podcast/tests tests/test_hopper_sensor.py tests/test_heal_chain_wiring.py tests/test_rotate_observations.py tests/test_comms_reconcile_sensor.py -q -p no:cacheprovider && python3 loopfactory.py validate --name podcast.",
      "check": "python3 /mnt/d_drive/repos/loop-factory/ringer/one-true-master/checks/fix_with_tests.py --verify-command 'PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/podcast/tests tests/test_hopper_sensor.py tests/test_heal_chain_wiring.py tests/test_rotate_observations.py tests/test_comms_reconcile_sensor.py -q -p no:cacheprovider && python3 loopfactory.py validate --name podcast' --patch /home/ankit114/ringer-work/dept-fix-r3b/podcast-w3b.patch --owned-files 'departments/podcast/runtime,departments/podcast/subgraphs.json,departments/podcast/procedural-graph.md,departments/podcast/tests,evidence.md' --min-new-tests 7",
      "expect_files": [],
      "verified": "podcast's three absent objectives gain additive configured-evidence sensing with every root pin green and publish_reliability semantics untouched"
    }
  ]
}
