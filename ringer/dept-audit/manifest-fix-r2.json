{
  "run_name": "department-audit",
  "workdir": "/home/ankit114/ringer-work/dept-fix-r2",
  "worktrees": true,
  "repo": "/mnt/d_drive/repos/loop-factory",
  "max_parallel": 2,
  "tasks": [
    {
      "key": "w2-podcast-v2-asks",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 4800,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo. Do NOT git commit/branch/push/stash. No MCP, no network, no skills; do not ask for approval — nobody is watching; fixture-only tests. NEVER read *.env/*secret*/*token*/*key*/*credential* files. NEVER edit charter.yaml or anything under runbooks/. TWO CONFIRMED AUDIT FINDINGS to fix in the podcast department (full report: ringer/dept-audit/reports/podcast-review.md; its line refs predate a recent podcast_daily.sh restructuring — re-locate before editing). FINDING A — v2 record coverage is mixed: factory/runrecord.py (read it fully) is the locked v2 contract; core sensors like runtime/pipeline_sensor.py already emit valid v2 rows, but (1) departments/podcast/runtime/record.py still appends a five-field legacy receipt to runs.jsonl only, (2) departments/podcast/runtime/comms_reconcile_sensor.py emits NO record at all, (3) departments/podcast/runtime/rotate_observations.py prints a loose receipt without a v2 append. FIX CONTRACT A (sidecar adoption, the proven sales pattern): every daily-invoked stage ALSO appends one validated v2 row via factory.runrecord (department podcast, node = the graph node id, status honest, external_actions_taken 0, cost {lane:'flat_subscription',model_calls:0}, artifacts referencing the stage's own receipt) — keep the legacy runs.jsonl writes untouched so existing consumers keep working, and say in evidence.md what would be needed to retire them later. A failed v2 append fails that stage (fail-closed). FINDING B — the escalation ask has no declared or executed return path: charter comms_loop_invariant requires it; runtime/escalate_outbox.py constructs a human question and writes the ask, but its node in departments/podcast/subgraphs.json has no emits_ask/return_path/return_sla_hours, AND the intended reader — the comms_reconcile node (runtime/comms_reconcile_sensor.py, already declared in subgraphs.json) — is NEVER INVOKED by the daily chain in runtime/podcast_daily.sh. FIX CONTRACT B: mark the escalate node emits_ask true with return_path naming the comms_reconcile component and return_sla_hours 48 ('# DEFAULT pending owner review' comment in the graph doc), update procedural-graph.md to match, and wire comms_reconcile_sensor.py into podcast_daily.sh as a receipt-gated stage (same receipt discipline as the other stages — nonzero or missing receipt halts per the fail-closed conventions already in that script; study its current structure incl. run_heal_phase and append_heal_failure before editing and preserve them EXACTLY). python3 loopfactory.py validate --name podcast must PASS (the emits_ask lint now applies to the marked node). TESTS: new file departments/podcast/tests/test_v2_adoption.py, >=6 fixture tests: each of the three fixed stages emits a row that factory.runrecord.validate_record accepts; a forced v2-append failure fails the stage; the daily chain invokes comms_reconcile receipt-gated (execute the real shell with stubs); the escalate node's graph declaration carries the return contract. EXISTING PINS: tests/test_heal_chain_wiring.py pins podcast_daily.sh structure — keep it green; update its assertions ONLY if your chain edit genuinely extends the pinned order, never weaken the fail-closed assertions. Write ./evidence.md. VERIFY: PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/podcast/tests -q -p no:cacheprovider && PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/test_heal_chain_wiring.py -q -p no:cacheprovider && python3 loopfactory.py validate --name podcast.",
      "check": "python3 /mnt/d_drive/repos/loop-factory/ringer/one-true-master/checks/fix_with_tests.py --verify-command 'PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/podcast/tests -q -p no:cacheprovider && PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/test_heal_chain_wiring.py -q -p no:cacheprovider && python3 loopfactory.py validate --name podcast' --patch /home/ankit114/ringer-work/dept-fix-r2/podcast-w2.patch --owned-files 'departments/podcast/runtime,departments/podcast/subgraphs.json,departments/podcast/procedural-graph.md,departments/podcast/tests,tests/test_heal_chain_wiring.py,evidence.md' --min-new-tests 6",
      "expect_files": [],
      "verified": "every podcast daily stage emits a validated v2 record fail-closed, and the escalation ask carries an executed return path through a chain-invoked reconcile reader"
    },
    {
      "key": "w2-social-v2-asks",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 4800,
      "spec": "Your current working directory IS a git worktree of the loop-factory repo. Do NOT git commit/branch/push/stash. No MCP, no network, no skills; do not ask for approval — nobody is watching; fixture-only tests, no live services, no real tokens. NEVER read *.env/*secret*/*token*/*key*/*credential* files. NEVER edit charter.yaml or anything under runbooks/. TWO CONFIRMED AUDIT FINDINGS to fix in the social department (full report: ringer/dept-audit/reports/social-review.md; social_daily.sh was recently patched to receipt-gate cap-yields and review-card creation — read its CURRENT state first and preserve those fixes exactly). FINDING A — legacy records: departments/social/runtime/record.py (~lines 106-160) builds {node, epoch, timestamp, shadow, payload_summary} rows appended to runs.jsonl; the ratified contract is factory/runrecord.py (read fully): validated v2 rows in runs-v2.jsonl with run identity, release, trigger, cost, status, receipts, external_actions_taken. FIX CONTRACT A (sidecar adoption, proven pattern): keep the legacy writer untouched for existing consumers, and ALSO append one validated v2 row per stage via factory.runrecord — department social, node = graph node id, one shared run_id per daily cycle across all its stage rows, external_actions_taken reflecting ACTUAL dispatch count that stage performed (0 in shadow paths), artifacts referencing the stage receipt; a failed v2 append fails the stage. FINDING B — review-card asks evade the ask schema and have no executed return loop: no node in departments/social/subgraphs.json declares emits_ask/return_path/return_sla_hours although the review-card stage (recently receipt-gated in social_daily.sh) creates an external Linear ask; reader/closer scripts exist at runtime/linear_read_comments.py and runtime/linear_close_issue.py but NOTHING in the department triggers them. FIX CONTRACT B: declare the review-card node emits_ask true, return_path naming the linear comment reader component, return_sla_hours 48 ('# DEFAULT pending owner review'); update procedural-graph.md to match; wire a receipt-gated harvest stage into social_daily.sh that invokes linear_read_comments in shadow-safe mode (no network in tests — the script must accept a fixture/injection path or stub boundary; study how existing social stages stub external calls in departments/social/tests and follow that pattern) and records asks older than return_sla_hours as an SLA breach receipt the manager can sense (escalation via the existing outbox conventions, not a new mechanism). python3 loopfactory.py validate --name social must PASS. TESTS: new file departments/social/tests/test_v2_and_asks.py, >=6 fixture tests: stage rows validate against factory.runrecord.validate_record; shared run_id across a cycle's rows; forced v2-append failure fails the stage; harvest stage runs receipt-gated in the chain (execute the real shell with stubs); an ask older than SLA produces the breach receipt; graph declaration carries the return contract. Write ./evidence.md. VERIFY: PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/social/tests -q -p no:cacheprovider && python3 loopfactory.py validate --name social.",
      "check": "python3 /mnt/d_drive/repos/loop-factory/ringer/one-true-master/checks/fix_with_tests.py --verify-command 'PYTHONDONTWRITEBYTECODE=1 python3 -m pytest departments/social/tests -q -p no:cacheprovider && python3 loopfactory.py validate --name social' --patch /home/ankit114/ringer-work/dept-fix-r2/social-w2.patch --owned-files 'departments/social/runtime,departments/social/subgraphs.json,departments/social/procedural-graph.md,departments/social/tests,evidence.md' --min-new-tests 6",
      "expect_files": [],
      "verified": "social emits shared-identity v2 records fail-closed and its review-card ask carries an executed, SLA-tracked return loop through the wired Linear reader"
    }
  ]
}
