{
  "run_name": "failed-loops-fix",
  "workdir": "/home/ankit114/ringer-work/failed-loops-fix-r1",
  "max_parallel": 3,
  "tasks": [
    {
      "key": "seo-ahrefs-window",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 3600,
      "engine_args": ["-c", "model_reasoning_effort=high"],
      "spec": "You are fixing ONE diagnosed defect in the SEO daily loop on this machine. Work directly on live paths. No MCP, no skills, no model calls beyond yourself. NEVER read files matching *.env, *secret*, *token*, *key*, *credential*.\n\nDIAGNOSIS (from a verified triage, read its full row in /mnt/d_drive/repos/loop-factory/estate/state/failed-loops-triage-2026-08-02.md under seo-loop-daily): the loop's evidence-reuse branch copies yesterday's Ahrefs evidence (cp -p preserving yesterday's generated_at) when AH_AGE_H <= 36, but declares TODAY'S window in the manifest, so the manifest gate rejects it with 'source ahrefs_ranking_summary window must end on or before generated_at' (manifest.py:203) and the whole daily run exits 2, discarding successful GSC/GA4/HubSpot/sitemap/blog pulls.\n\nSTEPS: (1) locate the repo: systemctl --user cat seo-loop-daily.service (ExecStart path); read live_refresh_run.sh's reuse branch and manifest.py:~203. (2) Fix per the triage recommendation: when reusing Ahrefs evidence, declare the REUSED source's original window (or mark it allow_historical if the manifest schema supports that) instead of today's window — smallest change that makes reuse self-consistent; do not weaken the gate for fresh evidence. (3) PROVE without running the full LLM loop: reproduce the failing condition in a temp dir (copy yesterday-style evidence with an old generated_at, build/validate the manifest the same way the script does) and show the gate now PASSES for reuse while a genuinely inconsistent window still FAILS (both cases executed, output captured). (4) If the repo is git, commit your change on the current branch with a clear message + 'Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>'; do not push. (5) Write ./evidence.md: repo path, diff of your change, both executed proof outputs, and the exact line of triage diagnosis you fixed.\n\nA checker fails you if evidence.md is missing, lacks a diff, or lacks BOTH executed outcomes (reuse-pass AND inconsistent-fail).",
      "check": "python3 -c \"import sys,re;from pathlib import Path;p=Path('evidence.md');t=p.read_text(errors='replace').lower() if p.exists() else sys.exit(print('FAIL: evidence.md missing') or 1);need=[('diff','(^|\\\\n)(---|\\\\+\\\\+\\\\+|@@)|diff --git'),('reuse pass proof','pass'),('negative proof','fail'),('window fix','window|allow_historical'),('repo named','\\\\.sh|\\\\.py')];fails=[n for n,pat in need if not re.search(pat,t)];print('SEO CHECK:','PASS' if not fails else 'FAIL');[print(' - missing',f) for f in fails];sys.exit(1 if fails else 0)\"",
      "expect_files": ["evidence.md"],
      "verified": "the Ahrefs reuse branch declares a self-consistent window: executed proof shows reuse passes the manifest gate and inconsistent windows still fail"
    },
    {
      "key": "codex-update-arch",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 2400,
      "spec": "You are fixing ONE diagnosed defect in the codex-update build script on this machine. Live paths. No MCP, no skills. NEVER read credential-pattern files.\n\nDIAGNOSIS (full row under codex-update in /mnt/d_drive/repos/loop-factory/estate/state/failed-loops-triage-2026-08-02.md): build-native.sh:62 selects the compiled native module with `find . -name \\\"*.node\\\" -type f | head -1` (unordered) and grabbed the npm-shipped darwin-arm64 prebuild instead of the freshly compiled Linux build/Release/*.node — so every update rolls back with 'NOT Linux x86-64 ELF runtime module'. The verification/rollback layer is CORRECT; only the selection is wrong.\n\nSTEPS: (1) locate build-native.sh via systemctl --user cat codex-update.service and read the selection code. (2) Fix selection per triage: prefer the electron-rebuild output path explicitly (build/Release/*.node) and/or filter candidates through `file` requiring 'ELF 64-bit LSB' + 'x86-64'; must generalize to every module it rebuilds (node-pty too), not just better-sqlite3. (3) PROVE without downloading a release: run the selection logic standalone against the existing extracted app/node_modules tree on disk (find where the last rolled-back extraction lives, or simulate: create a temp tree containing BOTH a fake darwin-arm64 .node and a real ELF .node — `file` must be the discriminator) — show old logic picks wrong, new logic picks the ELF, executed output for both. (4) Commit if in git (same footer), no push. (5) ./evidence.md: script path, diff, both executed selection outputs.\n\nChecker fails on missing diff or missing old-vs-new executed comparison.",
      "check": "python3 -c \"import sys,re;from pathlib import Path;p=Path('evidence.md');t=p.read_text(errors='replace').lower() if p.exists() else sys.exit(print('FAIL: evidence.md missing') or 1);need=[('diff','diff --git|(^|\\\\n)(---|\\\\+\\\\+\\\\+|@@)'),('ELF filter','elf'),('old-vs-new comparison','old|before'),('executed file output','x86-64|arm64')];fails=[n for n,pat in need if not re.search(pat,t)];print('CODEX-UPDATE CHECK:','PASS' if not fails else 'FAIL');[print(' - missing',f) for f in fails];sys.exit(1 if fails else 0)\"",
      "expect_files": ["evidence.md"],
      "verified": "the updater selects the Linux ELF build artifact deterministically, proven by executed old-vs-new selection against real files"
    },
    {
      "key": "sales-reactivation-repin",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 2400,
      "spec": "You are clearing ONE diagnosed release-drift block on this machine. Live paths. No MCP, no skills. NEVER read credential-pattern files. THE LANE IS SHADOW — confirm that before anything (its receipts/config must show shadow/dry-run; if you find evidence it can send externally, STOP and report instead).\n\nDIAGNOSIS (full row under sales-reactivation in /mnt/d_drive/repos/loop-factory/estate/state/failed-loops-triage-2026-08-02.md): the unit escalates 'release drift (1 mismatched artifacts)' — exactly one of 45 pinned artifacts drifted: departments/sales/runtime/content_catalog.json (edited 2026-07-26 08:26, release 7eb4b8754e7ebbee pinned 2026-07-24).\n\nSTEPS: (1) locate the repo via systemctl --user cat sales-reactivation.service. (2) SHOW THE DRIFT: diff the live content_catalog.json against the pinned release copy — include the full diff in evidence (it is a content catalog; if it unexpectedly contains anything person-identifying, summarize shape instead of quoting). (3) Decide by inspection: if the diff is additive/benign catalog content (new entries, copy edits), ACCEPT it by re-pinning with the repo's own release tooling (find how releases/ pins are cut there — mirror the existing release layout; do not invent a new format). If the diff looks like corruption or accidental damage, REVERT to pinned instead and say so. (4) Re-run: systemctl --user start sales-reactivation.service; verify via its receipt/journal that the drift escalation is GONE and the run is ok (quote the receipt line). (5) Commit any repo changes (same footer), no push. (6) ./evidence.md: repo, the diff, your accept-vs-revert decision + reasoning, re-pin evidence, post-fix receipt line.",
      "check": "python3 -c \"import sys,re;from pathlib import Path;p=Path('evidence.md');t=p.read_text(errors='replace').lower() if p.exists() else sys.exit(print('FAIL: evidence.md missing') or 1);need=[('drift diff shown','diff'),('decision stated','accept|revert'),('re-pin or revert evidence','pin|revert'),('post-fix run evidence','ok|success|drift')];fails=[n for n,pat in need if not re.search(pat,t)];print('SALES CHECK:','PASS' if not fails else 'FAIL');[print(' - missing',f) for f in fails];sys.exit(1 if fails else 0)\"",
      "expect_files": ["evidence.md"],
      "verified": "the sales lane's single drifted artifact was inspected, deliberately accepted or reverted, re-pinned, and the drift escalation cleared on a live run"
    },
    {
      "key": "sweeper-hash-repin",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 2400,
      "spec": "You are clearing ONE diagnosed hash-pin failure on this machine. Live paths. No MCP, no skills. NEVER read credential-pattern files.\n\nDIAGNOSIS (full row under open-loop-sweeper in /mnt/d_drive/repos/loop-factory/estate/state/failed-loops-triage-2026-08-02.md): the sweeper fails preflight every 30 min with 'runtime preflight hash mismatch' — /home/ankit114/scripts/open_engine_open_loop_launcher.py embeds a 7-file hash manifest (rendered 2026-07-17) and exactly one pinned file drifted: /home/ankit114/scripts/open_engine_approved_send_executor.py (edited 2026-07-22). ADDITIONAL CONTEXT the triage predates: /home/ankit114/scripts/linear_open_engine_mcp_adapter.py was ALSO deliberately edited today (2026-08-02, an authorized fix) — if the adapter is among the 7 pinned files, its hash needs updating too. The rebuild tool build_release_manifest.py is MISSING from ~/scripts.\n\nSTEPS: (1) read the launcher's embedded hash list; identify all pinned files and which hashes mismatch TODAY (compute sha256 yourself, show the table). (2) Search briefly for the missing tooling (locate build_release_manifest.py / release-manifest.json across /home/ankit114 and /mnt/d_drive/repos — bounded, 2 minutes); if found, use it; if not, update the embedded hash list in the launcher IN PLACE to the current file hashes — the list is the manifest. Add a dated comment naming which files' hashes were refreshed and why (executor edit 07-22 + adapter fix 08-02). (3) Re-run: systemctl --user start open-loop-sweeper.service; verify the preflight passes and quote the journal outcome (its normal work may create/refresh Linear cards — that is its owner-enabled job; report what it did, quote counts). (4) ./evidence.md: hash table before/after, the launcher diff, journal outcome.",
      "check": "python3 -c \"import sys,re;from pathlib import Path;p=Path('evidence.md');t=p.read_text(errors='replace').lower() if p.exists() else sys.exit(print('FAIL: evidence.md missing') or 1);need=[('hash table','sha256|hash'),('launcher diff','diff'),('preflight outcome','preflight|mismatch'),('journal evidence','journal|sweeper')];fails=[n for n,pat in need if not re.search(pat,t)];print('SWEEPER CHECK:','PASS' if not fails else 'FAIL');[print(' - missing',f) for f in fails];sys.exit(1 if fails else 0)\"",
      "expect_files": ["evidence.md"],
      "verified": "the sweeper's embedded hash manifest matches reality again (executor + adapter edits accounted for) and a live run passes preflight"
    },
    {
      "key": "hsd-instrument-guard",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 2400,
      "engine_args": ["-c", "model_reasoning_effort=high"],
      "spec": "You are adding INSTRUMENTATION + a SAFETY GUARD to the hsd lane — explicitly NOT fixing its root cause tonight (that needs the instrumentation's output first). Live paths, repo /mnt/d_drive/repos/hubspot-daily-2. No MCP, no skills. NEVER read credential-pattern files. HARD RULE: do not RUN gap_sync.py in any mode that could touch HubSpot — earlier tonight `gap_sync.py enforce --help` EXECUTED THE REAL ENFORCER because the subcommand ignores --help; that live footgun is half of what you are fixing. Prove your changes with code-level tests only.\n\nTWO CHANGES (diagnosis rows: hsd-daily-operator in /mnt/d_drive/repos/loop-factory/estate/state/failed-loops-triage-2026-08-02.md):\n1. ARGPARSE GUARD in scripts/gap_sync.py: every subcommand must honor --help (print usage, exit 0, run NOTHING) and reject unknown flags with exit 2, never proceeding. Read its current arg handling first and fix at the parsing layer.\n2. STDERR CAPTURE: the orchestrator/runner that invokes 'gap_sync.py enforce' (find the phase-2 invocation the triage describes) must persist the subprocess's stderr+traceback to logs/gap_sync_enforce-<date>.log and reference that path in its failure record, so the currently-invisible exception becomes diagnosable after the next 08:30 run.\n\nPROVE SAFELY: (a) a subprocess test calling 'gap_sync.py enforce --help' asserting usage text + exit 0 + a sentinel proving the enforcer body did NOT run (e.g. monkeypatch/env-guard or assert no network/no state change marker — design it so the test cannot touch HubSpot even if the guard fails: point any config/env at a nonexistent endpoint inside the test); (b) a test for unknown-flag rejection; (c) a test that a failing fake 'enforce' run leaves a stderr log file. Run the repo's existing test suite for the files you touched. Commit (same footer), no push. ./evidence.md: diffs, executed test output.",
      "check": "python3 -c \"import sys,re;from pathlib import Path;p=Path('evidence.md');t=p.read_text(errors='replace').lower() if p.exists() else sys.exit(print('FAIL: evidence.md missing') or 1);need=[('gap_sync diff','gap_sync'),('help-guard proof','--help|exit 0|usage'),('unknown-flag proof','unknown|exit 2'),('stderr log capture','stderr|log'),('executed tests','passed')];fails=[n for n,pat in need if not re.search(pat,t)];print('HSD CHECK:','PASS' if not fails else 'FAIL');[print(' - missing',f) for f in fails];sys.exit(1 if fails else 0)\"",
      "expect_files": ["evidence.md"],
      "verified": "the gap_sync --help footgun is disarmed and enforce failures now persist their traceback — proven by executed tests that cannot touch HubSpot"
    },
    {
      "key": "social-loop-diagnose",
      "engine": "claude",
      "task_type": "code-review",
      "timeout_s": 2400,
      "spec": "READ-ONLY diagnosis — you fix nothing, restart nothing, edit nothing; deliverable is ./report.md only. No MCP, no skills. NEVER read credential-pattern files; never quote draft content bodies (shapes/counts only).\n\nCONTEXT (row social-loop in /mnt/d_drive/repos/loop-factory/estate/state/failed-loops-triage-2026-08-02.md): the social department loop dies daily: every candidate draft is rejected by the CTA gate ('draft must contain exactly one distinct HTTP(S) cta_url', draft_post.py:470-477 in /mnt/d_drive/repos/loop-factory/departments/social/runtime/), the engine fallback chain then burns the whole TimeoutStartSec=600 budget and systemd SIGTERMs it. Triage says: inspect what the engines ACTUALLY emitted before touching gate or prompt.\n\nANSWER WITH EVIDENCE: (1) find the rejected draft artifacts/receipts from recent runs (state receipts, quarantine dir, logs) — for each attempt, HOW MANY distinct HTTP(S) URLs did the engine emit (0, 1-but-wrong, or >1)? Is the required cta_url present in the sanitized bundle the engine received? (2) Is the failure prompt-side (engine never told the exact URL to use / told to avoid links), gate-side (gate stricter than the prompt promises), or bundle-side (cta_url missing from input)? Quote the relevant prompt-construction lines from draft_post.py and the bundle fields (field names only). (3) The timeout: locate the engine-fallback loop; propose the internal per-engine budget the triage calls for (numbers: N engines x M s vs the 600s unit budget). (4) RECOMMEND the smallest fix (prompt line? gate tolerance? bundle field? per-engine timeout?) with file:line targets, each S/M effort — for a follow-up fix ring, not for you.\n\n./report.md: FIRST LINE 'DIAGNOSIS: PROMPT-SIDE' or 'DIAGNOSIS: GATE-SIDE' or 'DIAGNOSIS: BUNDLE-SIDE' or 'DIAGNOSIS: MIXED', then findings with Evidence lines (file:line, counts).",
      "check": "python3 -c \"import sys,re;from pathlib import Path;p=Path('report.md');t=p.read_text(errors='replace') if p.exists() else sys.exit(print('FAIL: report.md missing') or 1);f=t.strip().splitlines()[0].upper();ok=bool(re.match(r'^DIAGNOSIS: (PROMPT-SIDE|GATE-SIDE|BUNDLE-SIDE|MIXED)',f));low=t.lower();need=[('url counts','url'),('prompt evidence','prompt'),('timeout numbers','600|timeout'),('recommendation','recommend')];fails=([] if ok else ['bad first line'])+[n for n,pat in need if not re.search(pat,low)];print('SOCIAL-DIAG CHECK:','PASS — '+f if not fails else 'FAIL');[print(' - missing',x) for x in fails];sys.exit(1 if fails else 0)\"",
      "expect_files": ["report.md"],
      "verified": "the social CTA failure is attributed (prompt vs gate vs bundle) from real engine output evidence, with the smallest-fix targets named"
    },
    {
      "key": "eyecloud-extract-capture",
      "engine": "codex",
      "task_type": "code-fix",
      "timeout_s": 3600,
      "spec": "You are executing the triage-prescribed diagnostic for the Eye Cloud Pro nightly extraction. Live paths. No MCP, no skills. NEVER read credential-pattern files (the lane's own secret_exec wrapper handles auth — invoke through the SAME entrypoint the service uses). PHI RULE: this lane touches patient-adjacent data — your outputs may contain CATEGORY NAMES, COUNTS, and ERROR CLASSES ONLY; never patient records, names, or row contents; if an error message embeds record data, redact to its shape.\n\nCONTEXT (rows eyecloud-pro-extract-daily and eyecloud-pro-audit in /mnt/d_drive/repos/loop-factory/estate/state/failed-loops-triage-2026-08-02.md): nightly extract exits 1 with 16/35 categories ok; journal evidence is lost to retention; preflight auth is clean; leading hypothesis (unconfirmed) is a stale session after a 70-min hibernation gap at run start. The audit unit is purely downstream. The prescribed next step: one manual run with output captured, then read the per-category errors.\n\nSTEPS: (1) systemctl --user cat eyecloud-pro-extract-daily.service — identify the exact ExecStart. (2) Run that same entrypoint manually with ALL output captured: <ExecStart command> 2>&1 | tee ./extract-capture.log (run from the correct WorkingDirectory; expect ~10 min). (3) From the captured log + the lane's last_run.json, build the per-category table: category | ok/fail | error CLASS (auth/session, timeout, schema, empty, other) — counts and classes only. (4) VERDICT on the hibernation hypothesis: with a fresh awake-box run, did the previously-failing categories succeed (supports hypothesis) or still fail (refutes it — name the real error class)? (5) If a config-level fix is now obvious AND code-only (e.g. session refresh before extraction, retry-on-stale-session), implement it, run the entrypoint ONCE more to prove, commit (same footer) no push. If the fix needs credentials/infra, STOP and report. (6) ./evidence.md: the category table, hypothesis verdict, fix-or-stop decision, redaction note.",
      "check": "python3 -c \"import sys,re;from pathlib import Path;p=Path('evidence.md');t=p.read_text(errors='replace').lower() if p.exists() else sys.exit(print('FAIL: evidence.md missing') or 1);need=[('capture ran','extract-capture.log|categories'),('per-category table','category'),('hypothesis verdict','hypothes|hibernat'),('fix-or-stop decision','fix|stop|report')];fails=[n for n,pat in need if not re.search(pat,t)];print('EYECLOUD CHECK:','PASS' if not fails else 'FAIL');[print(' - missing',x) for x in fails];sys.exit(1 if fails else 0)\"",
      "expect_files": ["evidence.md", "extract-capture.log"],
      "verified": "the extraction failure finally has captured evidence: per-category error classes, a tested verdict on the hibernation hypothesis, and either a proven code fix or an honest stop"
    }
  ]
}
