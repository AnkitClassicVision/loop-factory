# outreach — concept map (LOCKED at F1, 2026-08-03)

Every concept traces to interview/intent-interview.md; the procedural graph
cites these ids.

- **C1 watched estates** — HSD (hsd-daily-operator, obe-fix-consumer) and
  the open-engine sales estate (daily chain, funnels, LinkedIn, enrichment,
  approval/send lanes) per the D1 SPLIT: podcast keeps its four obe-* guest
  watches; one unit, one watcher. (F1-A; D1, D3)
- **C2 state reconciliation** — funnel ledger state rebuilt against HubSpot
  evidence every run; any drift is a breach, not a warning. (F1-C; podcast
  charter precedent 2026-08-03)
- **C3 voice-gated drafting** — every draft passes the voice gate
  fail-closed; the department consumes PII-free gate receipts; a bypass is
  a kill condition. (F1-D; scoping round)
- **C4 approval queue + send classes** — everything sendable queues to the
  owner; sends occur only inside the three pre-declared classes, each
  separately promotable after burn-in; cold email excluded forever. (F1-B)
- **C5 escalation ask + return loop** — unknown failures escalate exactly
  once to the owner outbox; the return path is the reconcile reader on the
  next cycle (comms-loop invariant). (F1-D)
- **C6 objectives observation** — deterministic sensors write
  objectives-observed/v1: three hard zeros observed, rate metrics collected
  as baselines only until owner ratification. (F1-C)
- **C7 shadow discipline** — external_actions_taken=0 at every step until a
  class is promoted; the department itself never dispatches — the watched
  lanes do, under their own gateways. (Stage 1 destination)
