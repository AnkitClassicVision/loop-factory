# outreach department loop timer. Installed DISABLED by convention:
# generating a unit never enables it; a human enables it deliberately after
# a manual dry-run (the factory promotion-safety rule).
[Unit]
Description=Run the outreach department loop on schedule

[Timer]
OnCalendar=*:20/30
Persistent=false

[Install]
WantedBy=timers.target
