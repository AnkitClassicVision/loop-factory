# pulse department loop timer. Installed DISABLED by convention:
# generating a unit never enables it; a human enables it deliberately after
# a manual dry-run (the factory promotion-safety rule).
[Unit]
Description=Run the pulse department loop on schedule

[Timer]
OnCalendar=*-*-* 07:00:00
Persistent=false

[Install]
WantedBy=timers.target
