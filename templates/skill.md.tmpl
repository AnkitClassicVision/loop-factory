# Skill: <name>

Goal: <one line — the capability this skill provides>.
Used by: {name} department — runbook(s) <which>, graph node(s) <N#>.

## Contract

- Inputs: <declared>
- Output contract: <exact shape the caller can rely on>
- Executed QA check: <how a caller verifies this skill worked — command or assertion>
- Model contract: <none | engine/model/effort/cost ceiling, chosen from evidence>

## Procedure

1. <step>

## Boundaries

- Never: <hard limits — sends, writes, data classes this skill must not touch>
- Escalate when: <conditions>

## Manifest

- Version: v1. Changes to this skill used by a pinned release require re-pin
  (runbooks/process-change-qa.md).
